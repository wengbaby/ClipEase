## 本次优化

- 优化主窗口分组“颜色与图标”弹窗的首次显示流程，弹出前先完成内容尺寸测量，避免首次打开时位置偏移。
- 简化分组弹窗定位链路，统一使用按钮自身锚点，减少重启后和新增分组后的状态差异。

## 本次修复

- 修复重启轻贴后，第一次右键分组打开“颜色与图标”弹窗时没有显示在分组上方的问题。
- 修复新建分组后，第一次右键打开“颜色与图标”弹窗时位置不正确的问题。
- 修复关闭分组外观弹窗时可能触发 SwiftUI 状态更新时机警告的问题。

## 验证

- {{TEST_LINE}}
- 已构建 `.build/ClipEase.app`，版本为 `{{VERSION}} ({{BUILD}})`。
- 已生成并验证 `{{DMG_NAME}}`。
- 已挂载 DMG 检查内部 `ClipEase.app` 版本为 `{{VERSION}} ({{BUILD}})`。
- DMG SHA-256：`{{SHA256}}`。
