## 本次优化

- 搜索筛选标签按用户添加顺序显示，不再按类型或名称重新排序。
- 已添加的搜索筛选标签使用浅蓝背景和边框，当前选中的筛选使用深蓝背景，状态区分更清楚。
- 历史数据摘要分三行显示，分类占用按 MB、KB、b 自动选择单位，并加大行距。

## 本次修复

- 修复搜索框有光标时，空格仍触发卡片预览、Backspace 仍删除卡片的问题。
- 修复普通字符输入时搜索框极少数没有展开的问题。
- 修复性能/日志页面点击“保留 x 天”“最多 x MB”的上下按钮后应用退出的问题。

## 验证

- {{TEST_LINE}}
- 已构建 `.build/ClipEase.app`，版本为 `{{VERSION}} ({{BUILD}})`。
- 已生成并验证 `{{DMG_NAME}}`。
- 已挂载 DMG 检查内部 `ClipEase.app` 版本为 `{{VERSION}} ({{BUILD}})`。
- DMG SHA-256：`{{SHA256}}`。
