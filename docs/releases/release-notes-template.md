## 这版改了什么

- 简单说清这次用户能感知到的变化。
- 如果是修问题，就写具体场景，不写“能力升级”“体验焕新”这类空话。
- 如果只是小调整，就直说“小调整”，不要包装成大版本。

## 下载

下载下面的 `{{DMG_NAME}}`，打开后把 ClipEase 拖到 Applications 里即可。

## 验证

- {{TEST_LINE}}
- 已构建 `.build/ClipEase.app`，版本为 `{{VERSION}} ({{BUILD}})`。
- 已生成并验证 `{{DMG_NAME}}`。
- 已挂载 DMG 检查内部 `ClipEase.app` 版本为 `{{VERSION}} ({{BUILD}})`。
- DMG SHA-256：`{{SHA256}}`。
