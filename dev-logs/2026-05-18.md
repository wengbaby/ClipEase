# 2026-05-18 开发日志

## 今日目标

- 收口富文本编辑、全局提示 HUD、App 图标、分组颜色 / 图标弹窗和分组重命名等体验问题。

## 已完成

- 会话 `019e350a-646e-71e2-a944-936b9024cdbf`：围绕“颜色与图标”弹窗多轮修复取色器和输入焦点，最终确认采用草稿编辑语义。
- 会话 `019e350a-646e-71e2-a944-936b9024cdbf`：颜色 / 图标选择不再立即保存，只有点“确认”才写入分组；关闭弹窗或第二次 Esc 会丢弃当前草稿。
- 会话 `019e350a-646e-71e2-a944-936b9024cdbf`：大 / 小颜色方块改用系统原生 `NSColorWell`，同一色块再次点击可关闭系统取色器；第一次 Esc 仅关闭取色器，不保存颜色。
- 会话 `019e350a-646e-71e2-a944-936b9024cdbf`：图标搜索框聚焦时同步到底层键盘事件状态，Backspace 和 `Command+A/C/X/V/Z` 不再被主窗口快捷键抢走；Esc 有内容先清空，内容为空关闭弹窗并丢弃草稿。
- 会话 `019e350a-646e-71e2-a944-936b9024cdbf`：主窗口分组重命名框改为本地 AppKit 文本框并同步键盘事件状态；Esc 有内容先清空，内容为空退出且不保存，点击输入框外保存并退出。
- 移除主窗口右上角 `...` 的 SwiftUI `Menu` 默认倒三角，改为自定义无边框按钮触发菜单。
- 修复卡片右键来源 App 菜单：支持“忽略 App名 / 取消忽略 App名”，最终调整为忽略 App 时不删除当前卡片。
- 修复主窗口隐藏后 toast 锚点错误，富文本保存回调使用最近一次主窗口正常显示 frame。
- 按系统亮度 HUD 参考图重做全局提示样式：200x200、系统浅灰 / 动态毛玻璃、自动淡入淡出。
- 新建分组颜色和图标改为随机分配。
- 主窗口和设置页“颜色与图标”弹窗新增颜色方块取色入口，取色实时更新分组颜色，关闭 / 确认时同步关闭取色器。
- 对齐设置页和主窗口右键分组的“颜色与图标”弹窗布局：宽度、图标网格列数、滚动区域、标题、关闭、色板、搜索、确认按钮一致。
- 将颜色方块入口从 HEX 文本改为大 / 小颜色方块，并多轮修复点击命中。
- 将颜色方块最终改为系统原生 `NSColorWell`，提升主窗口 popover 场景下取色器稳定性。
- 将主窗口图标搜索框和分组重命名框改为本地 AppKit 文本框，支持输入、删除、原生快捷键和 Esc。
- “颜色与图标”改为草稿编辑：颜色 / 图标选择不会立即保存，点确认才保存；关闭或第二次 Esc 丢弃草稿。
- 新增 / 替换 App 图标资源，状态栏、设置页、主窗口标题、About、历史卡片来源图标、筛选来源图标、文件预览 fallback 等统一使用新图标。
- App 内图标展示统一圆角，并按 macOS 原生 App 图标比例调整。
- 重新生成带透明圆角的 `.icns`，解决 Finder / Dock 中 App 包图标仍显示方形的问题。
- 修复主窗口分组重命名、设置页分组名称输入、颜色弹窗、提示框位置和轻贴自身复制被忽略等问题。

## 验证结果

- 多次运行 `swift build`，通过。
- 多次运行 `git diff --check`，通过。
- 已运行 `verify_context_menu_editor_color_guards.py`、`verify_card_click_performance_guards.py`、`verify_history_selection_focus.py`、`verify_latest_clipboard_focus_convergence.py`、`verify_history_card_scroll_alignment.py`，通过。
- 已运行 `verify_group_appearance_color_panel.py`、`verify_history_window_interaction_toast.py` 以及文件卡片相关验证脚本，相关门禁通过。
- 会话 `019e350a-646e-71e2-a944-936b9024cdbf`：分组颜色 / 图标 / 输入框专项守护、主窗口选中定位守护和卡片点击性能守护均通过；最终运行包记录为 `1.0.123 (260519.0142)`。
- 多次执行 release 构建并启动 `.build/ClipEase.app`，记录到的版本包括 `1.0.105` 到 `1.0.126`。

## 遇到的问题

- 会话 `019e350a-646e-71e2-a944-936b9024cdbf`：透明覆盖层和手动 `NSColorPanel.orderFront` 在主窗口 / popover 场景下点击命中与焦点稳定性不足，改为 `NSColorWell` 后收敛。
- 会话 `019e350a-646e-71e2-a944-936b9024cdbf`：输入框未同步到底层 `HistoryKeyboardEventTap` 时，Backspace 容易被主窗口解释为删除卡片，已通过输入焦点状态保护修复。
- 主窗口 / popover 中手动打开 `NSColorPanel` 容易受焦点和临时视图重建影响，后续改为更稳定的 AppKit / NSColorWell 方案。
- Finder / Dock 图标来自 `.icns` 文件本身，仅运行时裁圆角无法影响应用包图标。

## 明日待办

- 继续验证主窗口和设置页的“颜色与图标”弹窗焦点、取色器、重命名和提示位置。
- 继续补充 `verify_group_appearance_color_panel.py` 防回归规则。

## 决策记录

- “颜色与图标”采用草稿编辑，确认才保存，关闭 / Esc 丢弃草稿。
- App 图标圆角必须写入 `.icns` 资源，不能只依赖运行时裁剪。
