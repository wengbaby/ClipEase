# 2026-05-13 开发日志

## 今日目标

- 继续第二版功能头脑风暴，只完善方案，不实施功能代码。

## 已完成

- 确认顶部分组栏在分组较多时使用横向滚动。
- 确认新建分组默认名称为“新分组”，默认随机颜色；新建后用户可立即修改名称、颜色和图标，交互参考 Paste。
- 确认删除分组默认一并删除组内内容，必须有明确确认。
- 确认敏感遮罩开启后，点击被遮罩内容需要通过系统密码或 Touch ID 验证后显示。
- 确认编辑记录不保留编辑前版本历史，直接更新原记录。
- 确认空分组可直接删除，有内容分组使用普通确认弹窗并显示会删除多少条且无法恢复。
- 修正敏感遮罩显示时长：验证通过后显示 30 秒。
- 确认分组图标来源为系统图标 + 轻贴内置图标库，暂不支持自定义图片。
- 确认分组顶部按钮默认不显示数量，悬停时显示数量。
- 确认分组支持拖拽排序。
- 记录第二版主窗口顶部简化方向：筛选放在搜索框右侧，`Command + F` 二次触发筛选，顶部不重复设置页状态项，右侧更多按钮只显示 `...`。
- 记录菜单快捷键统一方向：主窗口 `...` 菜单、菜单栏右键菜单和卡片右键菜单同步快捷键，卡片右键菜单按类别分割。
- 确认筛选面板来源 App 按最近使用排序。
- 确认类型筛选在面板中一次只能选择一种类型，但搜索框可同时承载多个筛选 token。
- 确认搜索框默认只显示搜索图标，点击搜索图标或直接输入后展开。
- 确认右键菜单“编辑”对文本、链接、颜色卡片显示，图片暂不显示编辑入口。
- 确认设备筛选默认不显示，只有未来同步预研相关功能开启后才显示。
- 确认筛选 token 支持关闭按钮和 Backspace 两种删除方式。
- 修正筛选面板记忆策略：筛选面板不长期记住上次打开位置和滚动位置。
- 确认颜色卡片编辑同时支持输入 HEX 和系统颜色选择器。
- 确认搜索框展开后自动聚焦，光标在末尾且不选中已有内容。
- 确认多个筛选 token 在搜索框内横向展示，超出宽度时横向滚动。
- 确认搜索框横向滚动时输入光标必须始终保持可见。
- 确认筛选 token 不支持拖拽排序，按固定顺序展示：类型、App、日期、分组。
- 确认颜色卡片编辑保存后只更新历史记录，不立即复制新颜色到剪贴板。
- 确认筛选 token 点击本体无动作，只能通过关闭按钮、Backspace 或筛选面板修改。
- 确认筛选 token 固定顺序中缺省类目后续前移，保持紧凑。
- 确认筛选面板每次打开回到顶部。
- 确认颜色编辑保存后显示全局提示“颜色已更新”。
- 确认搜索框中筛选 token 在前，关键词在后。
- 确认点击搜索框清空按钮时，同时清空关键词和全部筛选 token。
- 确认搜索框清空按钮在有关键词或筛选 token 时显示。
- 确认点击清空按钮后保持搜索框展开并聚焦；按 Esc 仍按分层关闭规则执行。
- 确认筛选面板每次打开回到顶部，且顶部不显示已选条件摘要。
- 确认颜色卡片编辑保存后只刷新颜色预览区域，不刷新卡片顶部标题栏颜色。
- 确认 Esc 新层级：先关闭筛选面板，再清空搜索内容和筛选 token，再关闭搜索框，最后关闭主窗口。
- 确认搜索框未展开时按 `Command + F` 展开搜索框并聚焦。
- 确认搜索框展开后点击主窗口内其他区域时保持展开。
- 确认筛选面板打开时点击主窗口卡片先关闭筛选面板，再选中卡片。
- 确认点击清空按钮后不显示全局提示。
- 确认搜索框展开且无关键词和筛选 token 时，点击主窗口外部直接关闭主窗口。
- 确认筛选面板打开时，点击主窗口外部同时关闭筛选面板和主窗口。
- 确认筛选面板打开时，再按 `Command + F` 关闭筛选面板并聚焦搜索框。
- 确认搜索框聚焦时左右方向键优先移动文本光标，上下方向键按系统文本框默认行为处理。
- 确认搜索框聚焦时按回车进入卡片选择。
- 确认搜索框光标在文本末尾时，按方向右键或方向下键进入卡片选择。
- 确认进入卡片选择后，搜索框保持展开，卡片显示选中态。
- 确认从卡片选择返回搜索框的方式为点击搜索框或按 `Command + F`。
- 确认搜索框聚焦时按 Tab 进入卡片列表，卡片列表中再次按 Tab 返回搜索框且光标回到原位置。
- 确认搜索框有中文输入法候选时，回车优先确认候选词；没有候选框时，回车进入卡片选择。
- 确认卡片选择状态下继续输入文字，会回到搜索框并追加输入。
- 确认卡片列表中按 `Shift + Tab` 反向切换卡片。
- 确认卡片选择状态下继续输入文字后，保留当前卡片选中态，直到搜索结果更新。
- 确认搜索结果更新后，自动选中第一条结果。
- 确认卡片选择状态下按 Delete 或 Backspace 删除选中卡片。
- 确认 Delete / Backspace 删除卡片不弹确认，直接删除。
- 确认删除当前卡片后选中右侧下一张。
- 确认删除卡片后显示全局提示“已删除”，并提供撤销入口。
- 确认删除收藏或分组里的内容时，从所有地方彻底删除。
- 确认删除撤销提示持续 8 秒，撤销后卡片恢复到原位置。
- 确认批量删除支持撤销。
- 确认删除图片卡片时立即删除本地图片附件。
- 确认收藏入口同时存在于右键菜单和预览窗口工具栏。
- 确认收藏卡片显示星标；置顶和收藏同时存在时，同时显示图钉和星标。
- 确认分组内容按加入分组时间倒序，收藏筛选中置顶在前，其余按收藏时间倒序。
- 确认从 `...` 新建文本创建的内容默认归属当前选中分组，且新建文本窗口允许选择分组。
- 确认链接 URL 编辑保存后后台重新抓取网页标题和图标，并立即复制新 URL 到剪贴板。
- 确认富文本编辑保存后尽量保留原富文本格式。
- 确认第二版第一阶段先做 SQLite 迁移验证。
- 确认撤销图片删除时需要恢复已删除的本地图片附件。
- 确认批量删除支持逐条撤销，撤销过程需要动画。
- 确认当前在“全部剪切板”分组时，新建文本进入普通历史。
- 确认普通历史“收藏到分组”后，原记录变成收藏并进入分组，不复制新记录。
- 确认取消收藏但内容仍属于分组时，保留分组归属。
- 确认收藏内容从分组 A 移到分组 B 时直接移动。
- 确认空状态文案：分组为空显示“暂无内容”，全部剪切板为空显示“复制内容后会显示在这里”，收藏为空显示“点击星标收藏常用内容”。
- 确认分组默认颜色来自固定预设色板，默认图标为随机图标。
- 确认分组重命名支持右键和双击，颜色/图标修改支持右键和分组管理面板。
- 确认 `...` 菜单保留“分组管理”。
- 确认第二版 SQLite 迁移验证直接切换 App 主存储。
- 确认迁移前不额外备份 JSON，迁移失败后清空数据重建，迁移成功后删除原 JSON。
- 确认数据库文件名为 `ClipEase.sqlite`。
- 确认 SQLite 附件策略仍为图片/富文本放文件夹，数据库只存索引。
- 确认 SQLite 搜索方案同时准备普通索引 + `LIKE` 和 FTS5，默认路径后续按验证结果确定。
- 确认 1,000 / 10,000 条测试数据入口后续只保留开发隐藏入口，并需要移出方案。
- 确认批量删除逐条撤销按删除前位置从左到右恢复。
- 确认取消收藏但内容仍属于分组时，分组内继续显示该内容，并标记为非收藏。
- 确认新建文本创建成功后打开主窗口，并显示和选中新记录。
- 确认链接 URL 编辑保存后显示全局提示“链接已更新并复制”。
- 确认 SQLite 默认搜索路径根据实际性能动态决定。
- 确认 1,000 / 10,000 条测试数据入口移到设置页隐藏入口。
- 确认 SQLite 迁移显示进度，设置页保留最近 5 次迁移结果。
- 确认第二版数据健康检查优先支持 SQLite。
- 确认第二版导入导出默认使用 SQLite 备份包。
- 确认取消收藏后的分组内非收藏状态使用空心星标。
- 确认 SQLite 搜索默认路径根据实际性能动态决定。
- 确认 SQLite 备份包扩展名为 `.clipeasebackup`。
- 确认迁移失败清空重建前不做二次确认，直接重建。
- 确认设置页隐藏测试数据入口通过连点版本号 5 次显示。
- 确认导出备份包时由用户选择是否包含图片、富文本等附件。
- 确认导出备份成功后显示提示，用户点击“显示”后再在 Finder 中定位文件。
- 确认导入备份遇到重复记录时弹窗让用户选择处理方式。
- 确认设置页保留最近 5 次迁移结果。
- 确认数据健康检查发现问题后提供“一键修复”。
- 确认顶部分组栏较多时横向滚动，并在右侧提供更多分组入口。
- 确认新建分组后不自动进入新分组，主窗口仍停留在当前分组。
- 确认有内容分组删除确认弹窗危险按钮文案为“删除分组和内容”。
- 确认收藏筛选为空时显示“点击星标收藏常用内容”。
- 确认敏感内容是否记录原内容由用户按规则选择。
- 确认链接编辑窗口只编辑 URL，不增加标题和备注字段。
- 确认右键菜单快捷键使用 macOS 符号格式。
- 确认全局提示层始终显示来源 App 图标。
- 确认第二版正式开始写代码时，第一步先做 SQLite 迁移和数据库基础。
- 确认备份包导出时“包含附件”默认勾选。
- 确认导入备份遇到重复记录时默认推荐“跳过重复”。
- 确认数据健康检查“一键修复”前每次都确认，完成后生成简短报告。
- 确认迁移失败直接清空重建时显示短提示。
- 确认更多分组入口点击右侧“更多”按钮弹出菜单。
- 确认更多分组超过 10 个时显示分组搜索。
- 确认分组颜色允许使用系统取色器自定义。
- 确认分组图标允许搜索。
- 确认收藏空心星标显示在卡片顶部标题栏右侧。
- 确认收藏星标点击后直接切换收藏状态。
- 确认 SQLite 备份包加密放到后续版本规划。
- 确认导入备份不先预览内容数量，直接导入。
- 确认备份包导入成功后只提示成功，不打开主窗口。
- 确认删除分组时不需要输入分组名确认。
- 确认分组管理窗口允许批量删除分组。
- 确认敏感规则由系统内置规则和用户自定义关键词共同组成。
- 确认敏感内容记录规则默认记录但遮罩。
- 确认 SQLite 基础完成后优先推进搜索 / 筛选重构。
- 确认第二版需要单独创建测试计划文档。
- 确认第二版新增文件卡片：复制 1 个本地文件时记录本地路径，卡片标题显示“1 个文件”，内容显示系统文件图标或缩略图，底部显示本地路径。
- 确认文件预览窗口优先使用 macOS Quick Look 能力展示图片、文档、PDF 等系统可预览内容；不支持预览时显示系统文件图标和基础信息。
- 确认文件卡片支持一次复制多个文件，标题显示“X 个文件”。
- 确认文件路径失效时，卡片底部路径变红，预览窗口显示文件图标和“文件已移动或删除”。
- 确认文件卡片支持收藏和加入分组。
- 确认文件卡片不支持导出文件副本，第二版只支持打开原文件位置。
- 确认文件卡片双击、回车或 `Command + 1-9` 时，优先粘贴文件引用；如果目标 App 不支持文件引用，则粘贴文件名。
- 确认文件卡片右键菜单增加“在 Finder 中显示”和“复制路径”。
- 确认文件卡片预览顶部标题显示文件名。
- 确认文件卡片底部路径最多显示 2 行，超出 2 行则省略。
- 确认文件卡片记录文件大小和文件修改时间，并在预览里显示。
- 确认文件卡片缩略图生成失败时显示系统文件图标。
- 确认文件卡片支持拖出到 Finder 或其他 App。
- 确认文件卡片参与搜索，搜索文件名和路径。
- 确认文件卡片不参与敏感遮罩。
- 确认 Quick Look 预览加载慢时显示加载中状态。
- 确认第二版测试计划文档为 `docs/V2_TEST_PLAN.md`，并在开始写 SQLite 前创建。
- 确认多文件卡片预览窗口展示文件列表。
- 确认多文件卡片双击、回车或 `Command + 1-9` 时粘贴全部文件引用。
- 确认文件路径失效后不提供“重新定位文件”。
- 确认多文件“复制路径”复制全部路径，每行一个。
- 确认文件卡片必须记录完整路径，不允许只记录文件名。
- 确认文件卡片路径不隐藏用户名，并显示文件扩展名。
- 确认多文件卡片主体缩略图采用堆叠多个文件图标的样式。
- 确认文件卡片 App 来源图标显示来源 App 图标，顶部颜色取来源 App 图标主色。
- 确认文件卡片搜索文件名和路径时支持拼音。
- 确认文件预览窗口内支持“打开文件”和系统分享。
- 确认文件卡片不支持删除原文件；删除历史记录不影响原文件。
- 确认多文件卡片中部分文件路径失效时，标记失效数量。
- 确认 Quick Look 预览失败时提供“在 Finder 中显示”。
- 确认文件卡片不记录安全作用域权限。
- 确认文件卡片不需要单独设置开关，默认记录。
- 确认文件卡片纳入第二版第一批内容增强实现。
- 确认第二版方案可以进入实现阶段，但在用户最终确认前不编写功能代码。
- 确认第二版第一批代码顺序为 SQLite 基础、Repository 抽象、测试计划补细、搜索 / 筛选重构。
- 确认第二版实现不新建开发分支，直接在 `main` 上小步提交。
- 确认第二版开始写代码后，每个阶段完成后再编译运行给用户测试。
- 确认第二版正式实现版本号从 `2.0.0(时间戳)` 开始。
- 新增第二版 Agent 协作规约，定义主控、数据层开发、UI / 交互开发、功能增强开发、测试、审查、验收和文档 / 日志 Agent 的职责、约束、调度流程和交付模板。
- 确认在用户于主控 Agent 中明确发起“开始开发第二版”前，不调用开发 Agent，不实现 SQLite，不修改业务代码。
- 完善第二版多 Agent 协作机制：新增 `docs/agents/` 技能卡体系、四级风险等级、红线确认格式、任务卡机器可读摘要、Agent 沟通记录、文件锁规则、优化 Backlog 和调度 Runbook。
- 确认主控 Agent 不写业务代码，但拥有调度、汇总、否决、要求返工和阶段放行权限。
- 确认高风险核心模块串行执行，低风险模块允许并行；高风险文件允许多人读但只能一人写，写锁由主控 Agent 发放和解除。

## 验证结果

- 本轮仅更新第二版方案文档，未实施功能代码，未打包新版本。
- 本轮新增协作文档并接入项目入口文档，未实施功能代码，未打包新版本。
- 本轮新增 Agent 技能卡、调度 Runbook 和优化 Backlog，并更新 README 与项目指引；未实施功能代码，未打包新版本。
- 用户已在主控 Agent 中明确发起“开始开发第二版”，第二版阶段 1 正式启动。
- 阶段 1 已新增 SQLite 基础存储、JSON 到 SQLite 迁移器、迁移结果记录和最近 5 次结果保存。
- 阶段 1 主存储已接入 SQLite：`ClipboardHistoryPersistence` 启动时优先迁移 / 初始化 `ClipEase.sqlite`，保存历史写入 SQLite。
- 阶段 1 迁移实现当前安全默认不删除源 JSON；真实用户目录中的删除 JSON、删除附件或清空 SQLite 仍需逐次红线确认。
- 已执行 `swift build`，SQLite 主存储接入后构建通过。
- 已用临时目录小样本验证 JSON 到 SQLite 迁移：2 条记录导入成功，图片附件索引 1 条，迁移结果 1 条，源 JSON 保留。
- 已用临时目录坏 JSON 验证失败重建路径：结果为 `failedRebuiltEmptyStore`，错误数 1，SQLite 空库可用，源 JSON 保留。
- 阶段 1 测试计划已补充小样本、损坏 JSON、空 JSON、重复记录、附件索引、1,000 / 10,000 条规模、失败重建、最近 5 次迁移结果和红线边界。
- 阶段 1 门禁未放行：验收 Agent 指出 10,000 条自动化、完整迁移矩阵、迁移进度 UI、设置页最近 5 次结果和测试目录删除 JSON 保护尚未完成。
- 架构守门和审查 Agent 指出迁移失败后空 SQLite 可能被当成可用主库，主控已返工：生产默认失败不重建空库，测试专用选项才允许失败重建，失败残留 SQLite 会被清理，App 回落读取 legacy JSON。
- 已重新执行 `swift build`，迁移失败保护返工后构建通过。

## 自动化补全记录

- 2026-05-13：本自动化仅执行 `python3 scripts/daily_log.py` 用于创建/补全日志文件；未在本轮自动运行 `swift build` 或各类验证脚本，请以实际终端记录为准并按需人工补充。
- 已重新用临时目录坏 JSON 验证失败保护：生产默认结果为 `failed` 且不留下 SQLite 主库；测试专用选项结果为 `failedRebuiltEmptyStore`，空库记录数 0，源 JSON 保留。
- 新增 `scripts/verify_sqlite_migration.py`，自动编译临时 Swift harness 并在测试临时目录验证 SQLite 迁移矩阵。
- `python3 scripts/verify_sqlite_migration.py` 已通过：小样本、空 JSON、重复记录、测试目录删除 JSON、坏 JSON 保护、最近 5 次迁移结果、1,000 条和 10,000 条均通过。
- 设置页历史数据区新增最近 5 次 SQLite 迁移结果展示，包含状态、时间、来源 / 导入 / 错误数量、失败重建和源 JSON 删除状态。
- 已重新执行 `swift build && python3 scripts/verify_sqlite_migration.py`，构建和迁移验证均通过。
- 新增启动期 SQLite 迁移进度窗口：检测到 `history.json` 需要迁移且 SQLite 不存在时，会在创建主 Store 前显示准备历史数据提示。
- 将 `ClipboardHistoryStore` 从 AppDelegate 属性初始化改为启动流程内初始化，便于迁移前显示进度提示。
- 新增 `--show-settings` 开发参数用于尝试打开设置页验证，不影响正常启动。
- 已用 bundle 方式启动 `.build/ClipEase.app`，真实默认目录非删除迁移成功：`ClipEase.sqlite` 主记录 152 条，附件索引 16 条，迁移结果 `succeeded|152|152|0|0`，`history.json` 仍保留。
- 已执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，均通过。
- 设置页最近 5 次结果展示已编译通过，但当前环境无法通过 AppleScript 控制菜单栏 App，`--show-settings` 未捕获到可见设置窗口截图，因此视觉人工核对仍建议由用户完成。

## 遇到的问题

- 暂无阻塞。

## 明日待办

- 等待阶段 1 架构守门、审查和验收报告；若有阻塞项先返工，再决定是否进入 Repository 抽象阶段。
- 阶段 1 数据层和迁移验证主体建议通过；进入 Repository 抽象前建议用户手动打开设置页历史数据区核对最近 5 次 SQLite 迁移结果展示。
- 用户截图确认设置页 SQLite 迁移结果展示正常，显示迁移成功、来源 152 条、导入 152 条、错误 0 个；阶段 1 放行。
- 阶段 2 Repository 抽象已启动并完成首轮实现：新增 `ClipboardHistoryRepository`、`ClipboardHistoryRepositoryKind`、SQLite Repository 和 JSON Repository。
- `ClipboardHistoryPersistence` 已改为附件管理 + Repository 门面，`ClipboardHistoryStore` 不直接依赖 SQLite 或 JSON 细节。
- 设置页和 AppDelegate 不再直接调用 SQLite 迁移器，迁移结果通过 `ClipboardHistoryPersistence.recentMigrationResults()` 暴露。
- `python3 scripts/verify_sqlite_migration.py` 增加 JSON Repository 验证，确认旧 JSON 实现仍可保存 / 读取。
- 已执行 `swift build && python3 scripts/verify_sqlite_migration.py && python3 scripts/smoke_check.py`，全部通过。
- 阶段 3 已启动：SQLite Repository 保持默认主存储，JSON Repository 保留为开发开关后端。
- SQLite 迁移成功后删除源 JSON 的代码路径改为显式开关 `history.deleteMigratedJSONAfterSQLiteSuccess` 控制，默认不删除真实 `history.json`，本轮未执行真实删除。
- SQLite 备份包已接入：导出备份包默认写入 `ClipEase.sqlite`，并保留 `history.json` manifest 兼容；导入备份包优先读取 SQLite，缺失时回退旧 manifest。
- 备份包导入遇到重复历史时弹窗提示，默认按钮为“跳过重复”。
- `python3 scripts/verify_sqlite_migration.py` 增加 SQLite 备份包导出 / 导入验证。
- 已再次执行 `swift build && python3 scripts/verify_sqlite_migration.py && python3 scripts/smoke_check.py`，全部通过。
- 阶段 3 剩余项：真实 JSON 删除需单独红线确认；导出备份包附件勾选 UI、SQLite 数据健康一键修复、设置页隐藏 1,000 / 10,000 条入口尚未完成。
- 导出备份包附件勾选 UI 已接入，默认勾选“包含图片和富文本附件”。
- 数据健康检查发现孤立附件时提供“一键修复”，修复前二次确认，修复后显示简短报告。
- 设置页关于区连点版本号 5 次后显示历史数据页性能测试入口，可生成 1,000 / 10,000 条测试数据并清理。
- 已执行 `swift build && python3 scripts/verify_sqlite_migration.py && python3 scripts/smoke_check.py`，全部通过。
- 阶段 3 除真实 `history.json` 删除红线外已完成；真实删除仍需用户单独确认。
- 用户明确回复“删除”后，已执行阶段 3 红线操作：删除真实 `~/Library/Application Support/ClipEase/history.json`。
- 删除前确认 `ClipEase.sqlite` 存在且 `clipboard_items` 为 154 条；删除后确认 `history.json` 不存在，`ClipEase.sqlite` 仍存在且记录数仍为 154。
- 阶段 4 收藏 / 永久保存已启动并完成主体实现：历史记录新增收藏状态、收藏时间、SQLite 自动补列、JSON / SQLite 备份包兼容保存。
- 主窗口卡片已显示收藏星标；置顶加收藏时同时显示图钉和星标。
- 历史卡片右键菜单和预览浮层头部均支持收藏 / 取消收藏。
- 顶部筛选新增收藏按钮和“收藏”筛选项；收藏视图中置顶优先，其余按收藏时间倒序。
- 保存期限清理已跳过收藏项；取消收藏后会立即重新应用保存期限，过期普通项会被清理。
- 已执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过；迁移验证脚本已覆盖收藏字段。
- 阶段 4 已重新通过 `scripts/build-app.sh --run` 打包并启动 `.build/ClipEase.app`；用户确认阶段 4 UI 没问题。
- 阶段 5 分组基础第一轮完成：新增分组模型、item 分组归属字段、Repository snapshot、JSON `groups.json` 和 SQLite `groups` / `group_items` 读写。
- 主窗口顶部新增分组栏，支持全部剪切板、横向分组按钮和 `+` 新建分组。
- 分组按钮右键支持重命名、预设颜色、预设图标和删除；有内容分组删除会确认“删除分组和内容”。
- 卡片右键支持“收藏到分组 / 移动到分组 / 移出分组”；普通历史进入分组时自动转为收藏。
- 已执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 23837，版本 `1.0.5(260513.1624)`。
- 阶段 5 后续分组管理完成：更多分组入口、超过 10 个分组搜索、分组管理 sheet、拖拽排序、批量删除、颜色自定义和图标搜索已接入。
- 已执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 54885，版本 `1.0.5(260513.1642)`。
- 用户反馈阶段 5 分组 UI 需要返工：分组应与搜索同排，`+` 和更多跟随分组末尾，新增 / 重命名内联输入，颜色 / 图标靠近当前分组悬浮，分组管理迁入设置页。
- 已按主控规约拆分任务卡并调度两个 UI / 交互开发 Agent：主窗口分组栏返工、设置页分组管理迁移。
- 主窗口分组栏已并入搜索行；`+` 和更多按钮紧跟可见分组；新增和重命名改为按钮内联输入；颜色与图标改为分组按钮 popover。
- 设置页新增“分组”分类，支持分组查看、新建、重命名、颜色、图标、拖拽排序、单个删除和批量删除。
- 已执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 83156，版本 `1.0.5(260513.1703)`。
- 用户继续反馈分组应位于收藏后面，且不要出现分割线；已调度 UI / 交互开发 Agent 做窄范围返工。
- 主窗口 toolbar 顺序已修正为搜索、筛选、置顶、收藏、分组区域；收藏与分组之间不再有分割线。
- 已执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 92117，版本 `1.0.5(260513.1710)`。
- 阶段 5 后续收口审查发现备份包未完整保留分组定义；已调度数据往返开发 Agent 修复。
- `.clipeasebackup` 的 SQLite 和兼容 `history.json` manifest 现在会导出分组；导入备份包返回 items + groups，Store 会保守合并分组并清理无效 `groupID`。
- 设置页备份导入 / 导出路径已接入 groups；普通“导出历史”也会把 `store.groups` 写入 JSON。
- 新建分组默认图标改为从内置图标库随机选择；设置页分组图标选择改为带搜索框的 popover 图标网格。
- 已重新执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过；迁移验证脚本已覆盖分组备份往返。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 55969，版本 `1.0.5(260513.1809)`。
- 用户反馈主窗口分组过多时不应隐藏，并要求置顶 / 收藏改为固定系统分组；已调度 UI / 交互开发 Agent 返工。
- 主窗口顶部已移除独立置顶 / 收藏圆形图标按钮，分组栏顺序调整为全部剪切板、置顶、收藏、用户分组、`+` 新建。
- 用户分组不再截断前 8 个或进入更多菜单，全部在同一个横向滚动分组栏展示；置顶 / 收藏系统分组可自定义图标和颜色，但不能删除或重命名。
- 已同步 `docs/RELEASE_CANDIDATE_REPORT.md` 当前构建号到 `1.0.5(260513.1809)`，解决 smoke check 版本报告不一致。
- 已重新执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 92143，版本 `1.0.5(260513.1844)`；RC 报告已同步到新构建号。
- 用户反馈主窗口顶部搜索和分组需要居中；已调度 UI / 交互开发 Agent 做窄范围布局修正。
- 顶部搜索按钮、筛选菜单和分组横向滚动区已放入居中宽度容器；分组内容不足时居中，超出时继续横向滚动，右侧更多菜单仍独立贴右。
- 已重新执行 `swift build` 和 `python3 scripts/smoke_check.py`，均通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 1629，版本 `1.0.5(260513.1850)`；RC 报告已同步到新构建号。
- 用户进一步确认顶部需要作为整体横向轨道：搜索 / 筛选 / 分组 / `+` 同轨道，内容不足居中，内容超出后搜索可向左滚出；关闭重开记住最后选中分组。
- 已调度 UI / 交互开发 Agent 返工：顶部轨道包含搜索框 / 搜索按钮、筛选菜单、全部剪切板、置顶、收藏、全部用户分组和 `+`；左侧标题状态与右侧更多菜单固定。
- 新建分组追加到用户分组末尾，`+` 始终在最后一个用户分组后面，新分组进入内联命名并滚动到可见位置。
- 选中分组使用 `@AppStorage` 记忆；如果记忆的用户分组不存在，则回到全部剪切板。
- 主控修正顶部轨道滚轮逻辑，避免复用卡片横向滚动记忆，防止顶部分组滚动污染卡片位置恢复。
- 已重新执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 20156，版本 `1.0.5(260513.1906)`；RC 报告已同步到新构建号。
- 阶段 6 更强搜索已启动，先进行只读架构 / 测试预研：当前搜索仍是 UI 内存过滤，SQLite 已有普通索引但尚未提供 LIKE / FTS 搜索 API。
- 已调度 UI / 交互开发 Agent 完成第一批搜索筛选 UI：新增搜索筛选状态、token 展示、筛选 popover 和 `Command + F` 三段行为。
- 搜索筛选第一批支持类型、来源 App、日期范围和分组组合条件；token 顺序固定为类型、App、日期、分组，清空按钮会同时清空关键词和 token。
- `HistoryPreviewItem` 已透传 `createdAt` 并让 `HistoryPreviewType` 可比较，用于日期和类型筛选。
- 阶段 6 未完成项：Backspace 删除 token、token 横向滚动和光标可见、SQLite LIKE / FTS 查询下沉、搜索验证脚本。
- 已重新执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 38037，版本 `1.0.5(260513.1919)`；RC 报告已同步到新构建号。
- 阶段 6 搜索筛选交互收口完成：移除顶部轨道旧“筛选”菜单，筛选入口统一收敛到搜索框右侧按钮。
- 搜索 token 区增加横向滚动；搜索输入为空且存在 token 时，按 Backspace 会删除最后一个 token 并保持搜索框焦点。
- 已重新执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 49901，版本 `1.0.5(260513.1926)`；RC 报告已同步到新构建号。
- 用户反馈搜索筛选 popover、分组外观 popover 点击会关闭主窗口；已修复外部点击关闭监控，App 自身 popover / 面板窗口不再触发主窗口关闭。
- 搜索框 token 体验已完善：有 token 无文字时隐藏 placeholder，token 更贴近输入内容，支持点击选中；空输入 Backspace 第一次选中最后 token、第二次删除。
- 搜索框展开宽度从 360 增至 520，并使用更明显的展开 / 关闭动画。
- 主窗口分组颜色与图标 popover 增加预设色块、实时预览和确认按钮；设置页分组管理改为完整“颜色与图标” popover，支持颜色、预设色和图标搜索调整。
- 已重新执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 85989，版本 `1.0.5(260513.1955)`；RC 报告已同步到新构建号。
- 搜索框按参考图继续返工：token 和文字输入合并为同一条横向滚动轨道，不再给 token 单独固定宽度。
- 搜索展开时，全部 / 置顶 / 收藏 / 用户分组按钮切换为纯图标紧凑模式，并隐藏新建分组 `+`；搜索关闭后恢复带文本和 `+`。
- 搜索筛选 popover 改为固定宽高按钮网格，内部超出时滚动；类型、App、日期、分组都改为带图标按钮，App 来源优先显示缓存 App 图标。
- 搜索筛选状态从单选改为同类多选集合，token 生成、删除和过滤逻辑已同步支持多选。
- ESC 顺序调整为先关闭筛选面板，再清空搜索文字与 token，再关闭搜索框，最后关闭主窗口。
- 已重新执行 `swift build`、`python3 scripts/verify_sqlite_migration.py`、`python3 scripts/smoke_check.py`，全部通过。
- 已执行 `scripts/build-app.sh --bump none --run` 打包并启动 `.build/ClipEase.app`，当前运行进程 49418，版本 `1.0.5(260513.2056)`；RC 报告已同步到新构建号。
- V2-BUGFIX-SEARCH-AUTH-ESC-001 / REWORK 阶段性放行后，主控按用户要求执行 `scripts/build-app.sh --bump none --run` 并启动 `.build/ClipEase.app`，当前运行进程 36250，版本 `1.0.5(260513.2203)`；RC 报告和 runbook 已同步到新构建号。
- 后续继续确认多文件卡片文件列表排序、预览列表是否显示每个文件大小、打开文件失败提示、系统分享失败回退和拼音搜索范围。

## 决策记录

- 会话 `019e1fff-95af-7b91-b7f0-7dcda89e9618`：阶段 5 分组栏返工以用户可见布局为准，分组区域必须紧跟收藏按钮，收藏和分组之间不保留分割线；本轮只修主窗口分组栏位置，不扩大到 SQLite、设置页或其它业务路径。
- 会话 `019e1353-6604-7d32-ae00-0253e09ef040`：新增 V2 测试计划 Agent 作为长期角色，专门维护 `docs/V2_TEST_PLAN.md`，阶段开始前先检查测试 / 验收门禁，Bugfix 后将原失败路径和相关守卫沉淀为回归项。
- 会话 `019e20c6-61ea-7b41-a922-90b3c78657cf`：阶段 6 搜索 / 授权 / ESC 返工后以运行包 `1.0.5(260513.2203)` 阶段性放行；搜索框、筛选 popover、授权提示和 Esc 层级后续仍需人工 UI 验收确认。
- 2026-05-13：第二版分组删除默认一并删除组内内容，因此实现时必须提供明确确认，避免误删。
- 2026-05-13：敏感遮罩查看采用系统密码或 Touch ID 验证，提高隐私保护强度。
- 2026-05-13：主窗口顶部走 Paste 式简洁路线，设置页已有状态不在主窗口顶部重复常驻展示。
- 2026-05-13：当前 SQLite 迁移策略风险较高，包含不额外备份、失败清空重建和迁移后删除 JSON；进入实现前需要再次确认。
- 2026-05-13：第二版多 Agent 协作采用主控 Agent 单点集成，各类 Agent 只在明确任务卡范围内工作；正式触发词为“开始开发第二版”。
- 2026-05-13：第二版 Agent 报告采用 Markdown 为主并嵌入 JSON 摘要，方便人工阅读和后续自动化处理。
- 2026-05-13：第二版红线操作包含删除 JSON、删除附件、清空 SQLite、导入备份、数据迁移、数据健康修复、批量删除、修改保存期限清理逻辑、修改收藏 / 分组删除逻辑、修改版本号、修改构建脚本和修改发布流程。
- 2026-05-13：新增 Bugfix Agent 作为长期保留角色；用户在主控 Agent 反馈 bug 后，主控只负责复现、归类、定级、生成任务卡和调度，不能亲自修改业务代码。
- 2026-05-13：新增 V2 测试计划 Agent 作为长期保留角色，专门维护 `docs/V2_TEST_PLAN.md`，按阶段补齐测试 / 验收门禁，并把 Bugfix 回归闭环沉淀进测试计划。
- 2026-05-15：新增 `docs/V2_FEEDBACK_AND_GUARDS.md`，要求主控 Agent 持久记录用户反馈和新增功能守卫；每次 bug 修复前必须查历史反馈和守卫项，避免修好一个问题又破坏其他已确认功能。
