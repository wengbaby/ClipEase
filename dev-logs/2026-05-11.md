# 2026-05-11 开发日志

## 今日目标

- 按用户反馈继续打磨新建文本编辑器窗口样式和输入可见性。

## 已完成

- 将新建文本窗口调整为约 `500 × 360` 的紧凑尺寸，贴近用户参考图。
- 缩小顶部工具栏、按钮和底部统计字号。
- 新增自定义富文本输入视图，拦截普通文本和富文本插入，强制输入内容使用黑色前景色。
- 在文本变化和样式操作后重新约束文本颜色，避免出现白字导致输入不可见。
- 修复新建文本编辑器基础交互：恢复标准 `NSTextView` 编辑行为，支持鼠标选择、键盘选择和 `Command + A`。
- 新增可拖动顶部工具栏，无边框窗口可通过顶部区域移动。
- 确认“取消”关闭窗口，“创建”保存文本；移除回车触发创建，避免影响正文换行。
- 将新建文本“创建”按钮改为蓝色背景白字。
- 调整暂停菜单：一级菜单改为“暂停 轻贴”；二级菜单主项会在暂停时显示“恢复”，定时暂停时显示 `时:分 恢复`。
- 暂停状态新增结束时间持久化，App 重启后可恢复定时暂停并到时自动恢复记录。
- 将“设置”从占位弹窗升级为基础设置窗口。
- 设置窗口第一版包含保存期限、暂停/恢复记录、自动粘贴权限入口、历史数量和清空历史。
- 将“帮助”从打开 GitHub 文档改为本地帮助窗口。
- 帮助窗口包含打开方式、历史卡片、搜索筛选、新建文本和权限说明。
- 新增全局快捷键控制器，使用原生 Carbon HotKey 注册默认 `Command + Shift + V`。
- 设置窗口新增快捷键状态展示，当前显示默认快捷键已启用。
- 新增开机自动启动控制器，使用 `SMAppService.mainApp` 管理登录项。
- 设置窗口新增“开机自动启动”开关，并显示当前系统登录项状态。
- 补齐卡片右键菜单：新增“粘贴”和“预览”。
- 新增空格键预览能力，选中卡片后按空格可打开/关闭预览浮层。
- 新增文字、链接和图片预览浮层，支持在预览中复制。
- 修复预览浮层布局：预览层改为绝对悬浮覆盖，不再参与卡片列表布局，避免卡片向下偏移。
- 预览箭头根据当前卡片坐标计算，指向被预览的卡片中心。
- 底部历史窗口高度调整为 760，为大图预览留出上方空间。
- 按用户反馈恢复底部历史窗口高度为 360。
- 将预览从底部窗口内部迁移为独立浮动 `NSPanel`，悬浮在底部窗口外部。
- 预览浮窗按屏幕坐标定位，箭头尖端对齐当前卡片顶部中心。
- 调整预览交互：空格打开预览，再按空格关闭预览。
- 预览打开时用键盘左右切换卡片，会同步切换预览内容。
- 鼠标点击其他卡片或切到其他窗口时关闭预览。
- 按 Esc 关闭底部历史窗口时，同步关闭预览悬浮窗口。
- 新增底部历史窗口从下往上出现的动画。
- 新增预览悬浮窗口从下往上出现的动画。
- 新增双击历史卡片粘贴，补齐卡片基础交互闭环。
- 新增忽略 App 设置模型，忽略列表本地保存到 `UserDefaults`。
- 剪贴板监听会跳过被忽略来源 App 的新复制内容。
- 卡片右键菜单新增“忽略来源 App”入口。
- 无法识别来源 Bundle ID 的记录不会显示忽略入口。
- 设置页新增“忽略 App”区域，可查看并移除已忽略 App。
- 更新产品需求和技术方案，记录忽略 App 的右键入口与本地存储实现。
- 新增全局快捷键设置模型，将快捷键按键和修饰键保存到 `UserDefaults`。
- 全局快捷键控制器支持设置变化后重新注册 Carbon HotKey。
- 设置页快捷键区域支持录制新快捷键、取消录制和恢复默认快捷键。
- 快捷键录制要求包含 `Command`、`Control` 或 `Option`，避免误录普通输入键。
- 更新产品需求和技术方案，记录快捷键自定义的实现方式。
- 菜单栏图标会根据记录状态变化：正常显示剪贴板图标，暂停时显示暂停图标，并更新提示文案。
- 自动粘贴前会先关闭轻贴底部窗口，再延迟发送 `Command+V`，减少粘贴被自身窗口抢焦点的问题。
- 主窗口支持 `Command+F` 打开并聚焦搜索框，搜索框回车可粘贴当前选中记录。
- 设置页忽略 App 区域新增“添加 App”，可通过系统选择面板选择 `.app` 并加入忽略列表。
- 设置页忽略 App 区域新增“清空”，可一次移除全部忽略 App。
- 忽略 App 设置新增重复判断返回值，用于提示“已在忽略列表”。
- 更新产品需求和技术方案，记录设置页选择 `.app` 添加忽略列表的实现。
- 新增十六进制颜色值识别，复制 `#RGB`、`RGB`、`#RRGGBB` 或 `RRGGBB` 会生成颜色卡片。
- 来源 App 获取改为优先使用当前活跃 App，并排除轻贴自身，减少来源误判。
- 右键菜单新增“复制纯文本”，用于文字、链接和颜色记录。
- 图片右键菜单新增“在 Finder 中显示”，可定位本地保存的图片文件。
- 启动时保存期限清理后会立即写回历史文件，避免过期记录重启后回流。
- 更新产品需求和技术方案，记录颜色识别、纯文本复制、Finder 显示和清理持久化规则。
- 新增来源 App 图标缓存目录 `AppIcons`。
- 剪贴板记录会缓存来源 App 真实图标，并在历史数据中保存图标文件名。
- 卡片右上角优先显示真实 App 图标，旧记录或缓存失败时回退到系统符号图标。
- 来源标题栏颜色优先从真实 App 图标计算平均主色，失败时回退默认规则。
- 搜索内容纳入来源 App 名称，便于按来源查找历史。
- 持久化新增可选 `iconFileName` 字段，旧历史记录可兼容加载。

## 验证结果

- 已执行 `swift build`，新建文本窗口尺寸和输入颜色修复调试构建通过。
- 已执行 `swift build`，新建文本选择、拖动和按钮交互修复调试构建通过。
- 已执行 `swift build`，创建按钮和暂停菜单状态改动调试构建通过。
- 已执行 `swift build`，基础设置窗口调试构建通过。
- 已执行 `swift build`，本地帮助窗口调试构建通过。
- 已执行 `swift build`，全局快捷键调试构建通过。
- 已执行 `swift build`，开机自动启动设置调试构建通过。
- 已执行 `swift build`，右键菜单和预览浮层调试构建通过。
- 已执行 `swift build`，预览浮层绝对定位和箭头跟随修复调试构建通过。
- 已执行 `swift build`，外部预览浮窗调试构建通过。
- 已执行 `swift build`，预览开关、键盘同步切换、点击关闭和窗口上滑动画调试构建通过。
- 已执行 `swift build && scripts/build-app.sh --run`，完成版本 `0.1.0(260511.0116)` 构建并启动应用。
- 已执行 `swift build`，双击粘贴与忽略 App 管理调试构建通过。
- 已执行 `swift build && scripts/build-app.sh --run`，完成版本 `0.1.0(260511.0122)` 构建并启动应用。
- 已执行 `swift build`，全局快捷键自定义调试构建通过。
- 已执行 `swift build && scripts/build-app.sh --run`，完成版本 `0.1.0(260511.0129)` 构建并启动应用。
- 已执行 `swift build`，菜单栏状态、自动粘贴前关闭窗口和搜索快捷聚焦调试构建通过。
- 已执行 `swift build && scripts/build-app.sh --run`，完成版本 `0.1.0(260511.0133)` 构建并启动应用。
- 已执行 `swift build`，设置页添加、清空忽略 App 调试构建通过。
- 已执行 `swift build && scripts/build-app.sh --run`，完成版本 `0.1.0(260511.0217)` 构建并启动应用。
- 已执行 `swift build`，颜色卡识别、复制纯文本、Finder 显示、来源 App 获取和清理持久化调试构建通过。
- 已执行 `swift build && scripts/build-app.sh --run`，完成版本 `0.1.0(260511.0224)` 构建并启动应用。
- 已执行 `swift build`，来源 App 真实图标缓存、卡片显示、图标主色提取和旧数据兼容调试构建通过。
- 已执行 `swift build && scripts/build-app.sh --run`，完成版本 `0.1.0(260511.0235)` 构建并启动应用。

## 遇到的问题

- `NSTextView` 在当前 SDK 中没有 `defaultTypingAttributes` 属性，已改为使用 `typingAttributes` 和自定义插入拦截。

## 明日待办

- 继续验证新建文本中文输入、样式按钮和保存后的富文本复制效果。

## 决策记录

- 新建文本窗口继续使用无边框自绘窗口，避免显示系统红黄绿标题栏。
