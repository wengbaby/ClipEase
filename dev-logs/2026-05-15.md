# 2026-05-15 开发日志

## 今日目标

- 同步 V2 主控规则，处理第七轮 / 第八轮主窗口与文件卡片反馈，补齐测试守卫和人工验收包。

## 已完成

- 会话 `019e1353-6604-7d32-ae00-0253e09ef040`：将“记住用户反馈和新增功能”从会话记忆改为项目持久规则，新增 / 启用 `docs/V2_FEEDBACK_AND_GUARDS.md` 作为反馈与功能守卫清单。
- 会话 `019e1353-6604-7d32-ae00-0253e09ef040`：明确主控 Agent 每次处理 bug、返工或新增功能前必须读取反馈守卫；Bug 修复任务卡必须包含关联历史反馈、关联功能守卫、受影响功能、不得回归项和最小回归测试。
- 同步主控 Agent 执行规则：主控不写业务代码、不亲自修 bug；阶段前调用测试计划 Agent；处理 bug / 返工 / 新功能前读取反馈守卫。
- 更新 Agent 协作文档、主控 Agent、Bugfix Agent、V2 测试计划 Agent、反馈守卫、Runbook、项目指引和优化 Backlog。
- 将第七轮 8 项反馈固化进 `docs/V2_TEST_PLAN.md` 回归门禁。
- 会话 `019e20c6-61ea-7b41-a922-90b3c78657cf`：记录维修第五轮运行态验收失败；虽然 Test / Review 静态门禁 PASS，但用户复现搜索外部关闭、半露卡片平移和新卡片强制滚动仍未达标，因此改按运行态可观察行为返工。
- 会话 `019e20c6-61ea-7b41-a922-90b3c78657cf`：将第六轮返工拆成两张小卡：搜索真正外部区域点击关闭且内部点击不关闭；半露卡片点击平移和新卡片加入后强制滚动可见。
- 修复空搜索框内部点击关闭、半露卡片点击平移、滚动后复制新内容定位、链接后台标题抓取、链接标题和 URL 间距、Finder 文件卡片、文本卡片底部淡化和图片卡片铺满等问题。
- 启动本地测试包供第七轮人工验收，未调用会改版本号的 `build-app.sh` / `bump_version.py`。
- 修复文件 pasteboard 语义：Finder 文件语义生成文件卡片，普通路径文本保持文本卡片。
- 会话 `019e2c24-6ebf-74e1-b7c2-94c0ac679105`：第七轮修复覆盖搜索内部点击、半露卡片、滚动后新剪切板定位、链接标题后台抓取、文件卡片识别、文本卡片底部淡化和图片铺满等 8 项用户反馈。
- 完成第八轮 8 项修复：链接卡片 footer 展示、半露卡片 / 新卡片定位、文件语义、文本卡片布局、富文本卡片 RTF 展示、分组右键选中等。

## 验证结果

- 已运行 `swift build`，通过。
- 已运行 `git diff --check`，通过。
- 已运行 `python3 scripts/smoke_check.py`，通过。
- 已运行搜索 / 滚动、文件捕获、文件粘贴、卡片 UI、链接标题后台抓取、第七轮卡片视觉、SQLite-only、无收藏 / 无管理模式等门禁，均通过。
- 第八轮全套门禁通过，包括滚动、文件语义、富文本 / 卡片布局、分组右键、无收藏 / 无管理模式、SQLite-only 等脚本。
- Test / Review / Acceptance 对第七轮和第八轮均给出 PASS，仅放行本地人工验收。

## 遇到的问题

- 卡片 / 富文本 Agent 曾因 429 中断，已拆成可接续小卡并由新 Agent 接手完成。
- 会话 `019e20c6-61ea-7b41-a922-90b3c78657cf`：第五轮静态验证通过不能替代用户运行态验收，后续类似 UI 行为必须以真实点击 / 滚动可见结果作为放行依据。
- `Resources/Info.plist` 中已有版本号 / 构建号脏改，Review 标记为最终发布或合并前需单独确认归因，不属于本轮功能放行范围。

## 明日待办

- 继续处理人工验收反馈。
- 继续对文件卡片、富文本卡片和主窗口定位做回归守卫。

## 决策记录

- 普通路径文本不能被文件卡片逻辑误判；只有系统文件 pasteboard 语义才生成文件卡片。
- 本轮本地验收包不改正式版本号。
- 会话 `019e1353-6604-7d32-ae00-0253e09ef040`：长期防回归不能依赖模型记忆，必须写入 Runbook、测试计划和反馈守卫，再由主控在每次修复前读取。
