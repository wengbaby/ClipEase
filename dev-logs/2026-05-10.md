# 2026-05-10 开发日志

## 今日目标

- 建立项目文档结构。
- 固化当前产品需求、技术方案、设计规范和开发步骤。
- 创建每日开发日志机制。

## 已完成

- 创建 `docs` 文档目录。
- 创建 `dev-logs` 开发日志目录。
- 创建项目入口指引文档。
- 创建产品需求文档。
- 创建技术方案文档。
- 创建设计规范文档。
- 创建开发步骤文档。
- 创建开发日志规范文档。
- 规划并细化第一版正式技术方案。
- 确认第一版采用 Swift + SwiftUI + AppKit 原生 macOS 技术栈。
- 明确 SQLite、本地图片文件、NSPanel 底部窗口、NSPasteboard 剪贴板监听、Accessibility 自动粘贴和 SMAppService 开机启动方案。
- 初始化 Swift Package 形式的 macOS App 工程。
- 实现最小菜单栏 App 壳：菜单栏图标、点击切换底部窗口、示例横向卡片、`Esc` 关闭窗口。
- 创建本地 `.app` 打包脚本 `scripts/build-app.sh`。
- 调大底部窗口高度和卡片尺寸。
- 初始化本地 git 仓库，并完成首次提交。

## 验证结果

- 已检查文档路径，`docs`、`dev-logs` 和 `scripts` 文件均已创建。
- 已运行 `python3 scripts/daily_log.py`，脚本可正常创建或补全当天日志。
- 已创建每日自动化任务，用于每天维护当天开发日志文件。
- 已运行 `swift build`，调试构建通过。
- 已运行 `scripts/build-app.sh`，release 构建通过并生成 `.build/ClipEase.app`。
- 已打开 `.build/ClipEase.app`，当前进程正在运行，等待用户测试菜单栏和底部窗口。

## 遇到的问题

- Swift 6 对 AppKit 和 SwiftUI 主线程隔离更严格，已通过给 App 壳控制器添加 `@MainActor` 解决。
- `gh` 命令未安装，当前只能完成本地 git 提交；连接 GitHub 远程仓库需要用户提供仓库 URL 或先安装并登录 GitHub CLI。

## 明日待办

- 手动打开 `.build/ClipEase.app` 验证菜单栏图标、底部窗口位置和 `Esc` 关闭行为。
- 进入阶段 2：完善底部横向窗口原型，包括真实选中态、键盘左右切换和更稳定的横向滚动。

## 决策记录

- 第一版采用小步推进方式开发。
- 第一版优先稳定实现菜单栏、底部窗口、剪贴板记录、搜索、粘贴、置顶、删除、暂停和设置。
- 第一版最低支持 macOS 13 Ventura。
- 第一版暂缓 iCloud、OCR、加密、富文本完整保留、多选粘贴、收藏贴板和文字编辑。
- 文字编辑功能必须在第二版实现，第一版预留入口和数据结构空间。
- 初期使用 Swift Package 承载源码并通过脚本生成 `.app`，后续需要签名、图标、发布流程时再升级为完整 Xcode 工程。
- 从本次开始，每次代码修改后执行构建并打开本地 App 供用户测试，随后提交到 git。
