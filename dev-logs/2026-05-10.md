# 2026-05-10 开发日志

## 今日目标

- 建立项目文档结构。
- 固化当前产品需求、技术方案、设计规范和开发步骤。
- 创建每日开发日志机制。

## 已完成

- 创建 `docs` 文档目录。
- 创建 `dev-logs` 开发日志目录。
- 创建项目入口指引文档。
- 创建产品需求文档。
- 创建技术方案文档。
- 创建设计规范文档。
- 创建开发步骤文档。
- 创建开发日志规范文档。
- 规划并细化第一版正式技术方案。
- 确认第一版采用 Swift + SwiftUI + AppKit 原生 macOS 技术栈。
- 明确 SQLite、本地图片文件、NSPanel 底部窗口、NSPasteboard 剪贴板监听、Accessibility 自动粘贴和 SMAppService 开机启动方案。
- 初始化 Swift Package 形式的 macOS App 工程。
- 实现最小菜单栏 App 壳：菜单栏图标、点击切换底部窗口、示例横向卡片、`Esc` 关闭窗口。
- 创建本地 `.app` 打包脚本 `scripts/build-app.sh`。
- 调大底部窗口高度和卡片尺寸。
- 初始化本地 git 仓库，并完成首次提交。
- 创建版本管理规则文档。
- 新增 `scripts/bump_version.py`，并让 `scripts/build-app.sh` 每次正式打包时自动递增构建号。
- 按用户要求调整版本显示规则为 `x.x.x(YYMMDD.HHMM)`。
- 推进阶段 2 底部横向窗口交互：卡片点击选中、选中态放大、方向键左右切换、增加横向示例数据。
- 修复选中卡片略微放大时贴边溢出的问题：缩小卡片尺寸、降低放大比例、增加横向列表安全留白。
- 推进阶段 3：新增内存版剪贴板历史 Store。
- 推进阶段 3：新增文字剪贴板监听器，使用 `NSPasteboard.general.changeCount` 轮询系统剪贴板。
- 将底部横向窗口从静态样例卡片切换为真实文字复制历史数据。
- 新增无历史记录时的空状态提示。
- 新增 URL 识别：复制 http/https 链接时记录为链接类型。
- 新增链接卡片样式：顶部显示“链接”，中间显示站点图标占位，底部显示域名和路径。
- 新增基础复制回剪贴板能力：双击卡片或按回车会把选中历史内容写回系统剪贴板。
- 新增内部写入跳过机制，避免从历史卡片复制回剪贴板后被监听器重复记录。
- 修复双击卡片无效问题：将单击和双击改为互斥手势，避免单击抢占双击事件。
- 新增右键菜单基础操作：复制、删除。
- 新增操作反馈：复制或删除后在顶部显示短暂状态提示。
- 优化卡片切换流畅度：去掉选中缩放，改为描边和轻量阴影变化。
- 优化方向键切换：切换时将选中卡片滚动到列表中心。
- 修复鼠标单击切换卡片延迟：取消单击/双击互斥等待，单击立即选中，双击作为高优先级复制手势。
- 修复左右方向键无法切换：将方向键监听恢复到主视图，并通过选中项变化触发滚动。
- 再次修复鼠标单击切换卡片延迟：使用 `DragGesture(minimumDistance: 0)` 在鼠标按下时立即选中，彻底绕开 `TapGesture` 的点击/双击等待。
- 为保证鼠标左键点击聚焦切换，暂时移除双击复制手势；复制保留在回车和右键菜单。
- 新增置顶/取消置顶能力：右键菜单可切换置顶状态，置顶内容排序到最左侧并显示图钉标识。
- 推进阶段 4：新增 Application Support 下的 JSON 持久化，保存文字/链接历史、置顶状态和排序信息。
- App 启动时从本地历史文件恢复记录；新增、删除、置顶后自动保存。
- 新增顶部搜索：点击搜索图标展开输入框，支持按文字、链接、标题和路径搜索。
- 新增顶部筛选菜单：支持全部、文字、链接、置顶。
- 新增搜索/筛选无结果空状态。
- 修复搜索框展开时内容区域下移：固定工具栏高度，并让搜索框隐藏时保留布局空间。
- 增强 `scripts/build-app.sh`：新增 `--run` 参数，打包后自动关闭旧 ClipEase 实例并启动新版本。
- 推进阶段 5：回车触发粘贴动作。若已有辅助功能权限，则复制后发送 `Command + V`；未授权时降级为仅复制。
- 推进阶段 5：工具栏新增自动粘贴权限状态，未授权时显示“需授权”，点击可打开系统辅助功能设置。
- 推进阶段 5：新增暂停/恢复记录开关，工具栏显示“记录中/已暂停”，点击后立即切换。
- 暂停状态已保存到 `UserDefaults`，重启 App 后仍保持用户上次选择。
- 剪贴板监听器在暂停时会更新系统剪贴板游标，但不会把暂停期间复制的内容写入历史。
- 推进阶段 6：新增真实图片剪贴板记录，图片保存到 `Application Support/ClipEase/Images`，历史 JSON 只保存文件名、尺寸和哈希。
- 图片卡片开始显示真实缩略图，回车或右键复制时可再次写回系统剪贴板。
- 筛选菜单新增“图片”，并修复删除/裁剪历史后去重索引未刷新的问题。

## 验证结果

- 已检查文档路径，`docs`、`dev-logs` 和 `scripts` 文件均已创建。
- 已运行 `python3 scripts/daily_log.py`，脚本可正常创建或补全当天日志。
- 已创建每日自动化任务，用于每天维护当天开发日志文件。
- 已运行 `swift build`，调试构建通过。
- 已运行 `scripts/build-app.sh`，release 构建通过并生成 `.build/ClipEase.app`。
- 已打开 `.build/ClipEase.app`，当前进程正在运行，等待用户测试菜单栏和底部窗口。
- 已执行新版 `scripts/build-app.sh`，确认构建号更新为时间戳 `260510.2029`。
- 已重新打开 `.build/ClipEase.app`，等待用户测试阶段 2 交互。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2039)`。
- 已重新打开 `.build/ClipEase.app`，等待用户验证卡片选中溢出修复。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2044)`。
- 已重新打开 `.build/ClipEase.app`，并用 `pbcopy` 写入测试文字，等待用户验证真实文字历史记录。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2049)`。
- 已重新打开 `.build/ClipEase.app`，并用 `pbcopy` 写入测试 URL，等待用户验证链接卡片。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2111)`。
- 已重新打开 `.build/ClipEase.app`，并用 `pbcopy` 写入测试文字，等待用户验证双击/回车复制回剪贴板。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2130)`。
- 已重新打开 `.build/ClipEase.app`，并用 `pbcopy` 写入测试文字，等待用户验证双击修复和右键菜单。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2139)`。
- 已重新打开 `.build/ClipEase.app`，并清理旧进程，只保留一个 ClipEase 实例运行。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2148)`。
- 已重新打开 `.build/ClipEase.app`，并写入多条测试内容，等待用户验证鼠标单击切换和左右方向键切换。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2205)`。
- 已重新打开 `.build/ClipEase.app`，并写入多条测试内容，等待用户验证鼠标按下即时选中。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2214)`。
- 已重新打开 `.build/ClipEase.app`，并写入多条测试内容，等待用户验证鼠标左键点击聚焦切换。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2223)`。
- 已重新打开 `.build/ClipEase.app`，并写入多条测试内容，等待用户验证右键置顶排序。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2231)`。
- 已验证生成 `~/Library/Application Support/ClipEase/history.json`，并在重启 App 后恢复历史记录。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2236)`。
- 已重新打开 `.build/ClipEase.app`，并写入文字和链接测试内容，等待用户验证搜索和筛选。
- 已再次执行 `swift build` 和 `scripts/build-app.sh`，构建通过，版本更新为 `0.1.0(260510.2247)`。
- 已重新打开 `.build/ClipEase.app`，等待用户验证搜索展开时内容区域不再下移。
- 已执行 `scripts/build-app.sh --run`，构建通过，版本更新为 `0.1.0(260510.2250)`，并验证只启动一个 ClipEase 实例。
- 已执行 `swift build` 和 `scripts/build-app.sh --run`，构建通过，版本更新为 `0.1.0(260510.2257)`，并验证只启动一个 ClipEase 实例。
- 已执行 `swift build` 和 `scripts/build-app.sh --run`，构建通过，版本更新为 `0.1.0(260510.2303)`，并验证只启动一个 ClipEase 实例。
- 已执行 `swift build` 和 `scripts/build-app.sh --run`，构建通过，版本更新为 `0.1.0(260510.2309)`，并验证只启动一个 ClipEase 实例。
- 已执行 `swift build`，图片记录改动调试构建通过。
- 已执行 `scripts/build-app.sh --run`，构建通过，版本更新为 `0.1.0(260510.2315)`，并验证只启动一个 ClipEase 实例。
- 已通过脚本写入测试图片到系统剪贴板，确认生成图片历史记录、图片尺寸和本地 PNG 文件。

## 遇到的问题

- Swift 6 对 AppKit 和 SwiftUI 主线程隔离更严格，已通过给 App 壳控制器添加 `@MainActor` 解决。
- `gh` 命令未安装，当前只能完成本地 git 提交；连接 GitHub 远程仓库需要用户提供仓库 URL 或先安装并登录 GitHub CLI。

## 明日待办

- 根据用户反馈调整搜索框、筛选菜单和真实历史记录展示。
- 根据用户反馈调整链接卡片样式。
- 继续阶段 3：完善去重策略、来源 App 显示和文字/链接历史记录稳定性。
- 继续补齐右键菜单：预览。
- 后续统一使用 `scripts/build-app.sh --run` 打包并重启本地 App，避免测试时残留多个 ClipEase 进程。
- 鼠标左键点击聚焦切换是优先级最高的基础交互；如与双击复制冲突，双击复制让位给回车和右键复制。
- 继续阶段 5：优化授权后的状态刷新和权限说明文案。
- 后续补充暂停记录的菜单栏右键入口，以及可选的定时暂停功能。
- 继续阶段 6：补充图片预览浮层、图片右键菜单细项，以及真实用户截图场景验证。
- 阶段 4 当前先用 JSON 验证持久化链路，后续图片和保存期限稳定后迁移到 SQLite。

## 决策记录

- 第一版采用小步推进方式开发。
- 第一版优先稳定实现菜单栏、底部窗口、剪贴板记录、搜索、粘贴、置顶、删除、暂停和设置。
- 第一版最低支持 macOS 13 Ventura。
- 第一版暂缓 iCloud、OCR、加密、富文本完整保留、多选粘贴、收藏贴板和文字编辑。
- 文字编辑功能必须在第二版实现，第一版预留入口和数据结构空间。
- 初期使用 Swift Package 承载源码并通过脚本生成 `.app`，后续需要签名、图标、发布流程时再升级为完整 Xcode 工程。
- 从本次开始，每次代码修改后执行构建并打开本地 App 供用户测试，随后提交到 git。
- 版本策略：完整显示为 `x.x.x(YYMMDD.HHMM)`；每次正式打包自动更新时间戳构建号；阶段性功能用 minor，修复用 patch，重大升级用 major。
