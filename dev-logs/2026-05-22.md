# 2026-05-22 开发日志

## 今日目标

- 继续性能 / 动画优化，优先收口新增卡片定位、滚动重试和后台资源预热路径。

## 已完成

- 新增卡片定位 / 最新卡片滚动重试从递归创建多条 16ms MainActor 任务，改为单个可取消的 `latestFocusRetryTask` 循环。
- 窗口隐藏或最新卡片定位完成时会取消 `latestFocusRetryTask`，避免连续复制、频繁开关窗口或布局未稳定时叠加多条无效滚动重试。
- 更新 `scripts/verify_card_click_performance_guards.py`，守卫最新卡片定位必须使用可取消重试任务，避免回退成多条递归重试链。
- 已执行 `python3 scripts/verify_card_click_performance_guards.py`、`python3 scripts/verify_history_window_animation_performance.py`、`swift build` 和 `./scripts/build-app.sh --bump patch --run`。
- 后台资源预热现在复用卡片可见加载的 `HistoryCardAssetLoadGate`，图片缩略图、App 图标和富文本预览预热都走同一 3 并发资源门，避免预热与可见卡片加载同时抢磁盘 IO / 图片解码 / 富文本解析资源。
- 预热仍保持全量 `filteredItems` 分批、低优先级、可取消处理；富文本预热复用 `RichTextCardPreviewCache.loadAttributedString`，避免维护两套解析逻辑。
- 更新 `scripts/verify_history_preheat_all_items.py` 和 `scripts/verify_card_click_performance_guards.py`，守卫全量预热、共享资源门和卡片加载门。
- 已执行 `python3 scripts/verify_history_preheat_all_items.py`、`python3 scripts/verify_card_click_performance_guards.py`、`swift build` 和 `./scripts/build-app.sh --bump patch --run`。
- 使用 `ui-ux-pro-max` 参考交互反馈规则，按 macOS 轻量状态层思路优化历史卡片 hover / press 动画：悬停时增加极轻白色状态层和 `1.004` 缩放，按下时使用 `0.996` 缩放和更明确状态层。
- 卡片状态反馈通过 AppKit overlay 的 tracking area 与 mouse down/up/drag/right-click 回调传回 SwiftUI；松开、移出、右键或开始拖拽都会复位，避免按下态卡住。
- 动画保持 80-120ms，只使用 opacity / scale，不动画宽高或重阴影，避免影响横向滚动性能。
- 更新 `scripts/verify_card_click_performance_guards.py`，守卫 hover / press 状态、轻量动画、tracking area 和复位回调。
- 已执行 `python3 scripts/verify_card_click_performance_guards.py`、`swift build` 和 `./scripts/build-app.sh --bump patch --run`。

## 运行版本

- 当前运行版本：`2.0.67 (260522.0048)`
- 当前运行 PID：`97879`

## 已知说明

- 编译仍有既有 Vision / PDFKit Sendable warning，本次未改动该路径。
