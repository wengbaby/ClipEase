# 2026-05-14 开发日志

## 今日目标

- 推进 V2 阶段 8 主窗口体验细节，收口新剪切板定位、卡片右键选中、搜索外点关闭、视口恢复、Stage 9 文件卡片前置方案和数据基础。

## 已完成

- 修复新剪切板加入后再次打开主窗口未自动定位 / 选中新卡片的问题。
- 修复右键卡片时未显示选中外边框的问题。
- 修复卡片顶部栏遮挡选中外边框的问题。
- 完成阶段 8 第一批主窗口体验实现，并通过 Test / Review / Acceptance 门禁。
- 完成阶段 8 第二批体验修复：新内容时放弃旧横向滚动位置、过滤后视口重置、点击主窗口外部关闭、搜索内点击不关闭。
- 完成 Stage 9 文件卡片 / Quick Look 前置方案，收窄第一批 MVP 范围：文件路径记录、文件卡片展示、路径失效标记、Finder 打开、复制路径和基础预览。
- 完成 Stage 9 文件卡片数据基础：`.file` 类型最小数据结构、SQLite-only 基线守卫和 Stage 9 数据基础验证。
- 完成文件捕获第一批：Finder / 系统文件语义复制可生成文件卡片，普通文本路径不误判为文件。
- 完成文件卡片 UI / 行为第一批：文件名、数量、路径、系统图标 / 缩略图展示，基础打开和复制路径能力。

## 验证结果

- 已运行 `swift build`，通过。
- 已运行 `python3 scripts/smoke_check.py`，通过。
- 已运行 Stage 8 / Stage 9 相关验证脚本，包括历史窗口选择、视口恢复、文件捕获、文件粘贴、文件卡片 UI 和 SQLite-only 基线守卫，相关门禁通过。
- 多次执行 `scripts/build-app.sh --bump none --run` 启动 `.build/ClipEase.app` 供本地测试，记录到的构建包括 `1.0.11`、`1.0.12`、`1.0.13`、`1.0.14` 及后续 Stage 9 本地构建。
- 若 smoke check 因 RC 报告版本未同步 HOLD，已由文档同步后重跑通过。

## 遇到的问题

- 部分验证脚本仍含旧收藏 / 管理模式字段，触发 SQLite-only 基线扫描，需要同步改为新基线。
- Stage 9 初期只放行数据基础和基础文件卡片能力，完整 Quick Look 深集成仍待后续阶段。

## 明日待办

- 继续收口 Stage 9 文件卡片人工验收反馈。
- 继续清理旧收藏、旧管理模式、旧 JSON 迁移相关门禁和脚本。
- 继续按主控 Agent 规约记录 Test / Review / Acceptance 结果。

## 决策记录

- Stage 9 第一批文件卡片采用收窄 MVP，不直接做完整 Quick Look 深集成。
- 文件捕获必须以 Finder / 系统 pasteboard 文件语义为准，普通路径文本不能误判为文件卡片。
