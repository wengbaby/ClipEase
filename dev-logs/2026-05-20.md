# 2026-05-20 开发日志

## 今日目标

- 继续推进第二版整体项目，收口 Stage 10 iCloud 预研的静态门禁，并保持可构建可运行。

## 已完成

- 补充 Stage 10 iCloud 预研静态门禁脚本：文档完整性、CloudKit runtime 禁止项、schema 禁止变更、附件上传禁止项、用户确认问题覆盖。
- 将 Stage 10 preflight 静态检查从“建议方向”收口为 `docs/V2_TEST_PLAN.md` 中的正式门禁，并在 `docs/V2_AGENT_RUNBOOK.md` 记录验证结果。
- 执行 `python3 scripts/verify_stage10_icloud_preflight_docs.py`、`python3 scripts/verify_stage10_no_cloudkit_runtime.py`、`python3 scripts/verify_stage10_no_schema_change.py`、`python3 scripts/verify_stage10_no_attachment_upload.py`、`python3 scripts/verify_stage10_user_questions.py`，全部通过。
- 执行 `python3 scripts/verify_sqlite_only_baseline.py`、`python3 scripts/verify_no_management_no_favorite_ui.py`、`git diff --check`，全部通过。
- 执行 `swift build --product ClipEase`，通过。
- 执行 `./scripts/build-app.sh --bump none --run`，成功打包并启动 App。

## 验证结果

- Stage 10 静态门禁全部通过。
- SQLite-only 基线和无收藏 / 无管理模式守卫通过。
- Swift 编译通过，App 已启动可供人工测试。

## 遇到的问题

- `scripts/build-app.sh --run` 会更新 `Resources/Info.plist` 的 `CFBundleVersion` 时间戳，这是脚本当前行为。
- 当前工作区仍有一处未提交的 `Sources/ClipEase/Features/HistoryWindow/HistoryCardView.swift` 改动，属于你之前撤回阶段9相关内容后留下的状态，我没有混入本轮推进。

## 明日待办

- 继续确认 Stage 10 下一步是仅补预研文档，还是进入单独 spike 任务卡。
- 视用户确认情况决定是否继续处理当前残留的 `HistoryCardView.swift` 回退点。

## 决策记录

- “静态门”就是不碰真实运行时、只做代码 / 文档层的边界检查，用来保证 Stage 10 还停留在预研范围内。
- 第二版当前继续遵守 SQLite-only、无收藏、无管理模式、无多选、无批量操作基线。
