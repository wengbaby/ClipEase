# 2026-05-19 开发日志

## 今日目标

- 修复分组重命名、设置页分组输入、颜色与图标取色器、默认色板、popover 定位和图标选中态问题。

## 已完成

- 会话 `019e3c31-4044-7f71-878f-9cd263d74e64`：统一 App 图标资源，替换 `Resources/ClipEase.icns`、`design/AppIcon.icns` 和 `design/AppIcon.iconset`。
- 会话 `019e3c31-4044-7f71-878f-9cd263d74e64`：状态栏图标不再使用 SF Symbol，改为从 App 图标生成小图标；暂停状态叠加状态标记。
- 会话 `019e3c31-4044-7f71-878f-9cd263d74e64`：设置页侧栏、主历史窗口标题、About 弹窗、历史卡片来源 App 图标、搜索筛选来源图标、文件预览 fallback 图标和 Quick Look fallback 文件图标统一圆角展示。
- 会话 `019e3c31-4044-7f71-878f-9cd263d74e64`：图标圆角比例改为接近 macOS 原生 App 图标，按边长约 `22.5%` 计算，而不是固定小半径。
- 会话 `019e3c31-4044-7f71-878f-9cd263d74e64`：重新从 iconset 生成带 alpha 透明圆角的 `.icns`，解决 Finder / Dock 显示仍为方形的问题；验证 `.build/ClipEase.app/Contents/Resources/ClipEase.icns` 带 alpha。
- 会话 `019e3c41-775a-7592-8718-c4e650982322`：默认颜色数组实际从 18 个补到 20 个，新增 `#BF5AF2` 和 `#64D2FF`，用于默认色板和新建分组随机颜色来源。
- 会话 `019e3c41-775a-7592-8718-c4e650982322`：修复新建分组后首次打开“颜色与图标”弹窗定位偏移；持久 popover 监听锚点 frame / origin / window 变化，并在打开后延迟重定位一次。
- 会话 `019e3c41-775a-7592-8718-c4e650982322`：主窗口和设置页“颜色与图标”图标网格选中态统一为蓝底白图标，未选中保持原浅色背景。
- 修复主窗口分组双击无法进入重命名：改用 AppKit mouse event 识别双击，并延迟到当前点击事件结束后进入重命名。
- 修复重命名时点击自身输入框会关闭的问题：新增输入框屏幕区域排除。
- 设置页“分组”名称输入框改为草稿编辑：输入时只更新草稿，失去焦点或回车才保存，Esc 取消并回退。
- 修复点击 42x42 色块会关闭“颜色与图标”和取色器的问题：将弹窗改为 AppKit `NSPopover` 持久行为，取色器打开时不因失焦关闭。
- 修复主窗口分组右键“颜色与图标”中的 42x42 色块无法弹出取色器的问题。
- 取色器根据 42x42 色块屏幕位置靠近“颜色与图标”弹窗弹出，并提升层级。
- 默认颜色数组从 18 个增加到 20 个，新增 `#BF5AF2` 和 `#64D2FF`。
- 修复新建分组后首次打开“颜色与图标”弹窗定位偏移：持久 popover 监听锚点 frame / origin / window 变化，并在打开后重定位。
- 将主窗口和设置页“颜色与图标”图标网格的选中态改为蓝底白图标。

## 验证结果

- 多次运行 `swift build`，通过。
- 已运行 `python3 scripts/verify_group_appearance_color_panel.py`，通过。
- 已运行 `python3 scripts/verify_history_window_interaction_toast.py`，通过。
- 已运行 `git diff --check`，通过。
- 会话 `019e3c31-4044-7f71-878f-9cd263d74e64`：已执行 `swift build --product ClipEase` 和 `./scripts/build-app.sh --bump patch`，最终确认过 `1.0.129 (260519.2040)` 包内 `.icns` 带透明圆角。
- 会话 `019e3c41-775a-7592-8718-c4e650982322`：默认色板、popover 定位和图标选中态修改均通过 `swift build`、`verify_group_appearance_color_panel.py`、`git diff --check`，并以 App bundle 方式启动。
- 多次执行 release 重新打包签名并启动 `.build/ClipEase.app`，记录到的构建包括 `1.0.127`、`1.0.128`、`1.0.129`。
- 最终记录构建：`1.0.129 (260519.2057)`。

## 遇到的问题

- 会话 `019e3c31-4044-7f71-878f-9cd263d74e64`：运行时裁圆角只影响 App 内展示，Finder / Dock 图标来自 `.icns` 资源本身，必须重建带透明圆角的图标资源。
- 会话 `019e3c41-775a-7592-8718-c4e650982322`：新建分组后顶部横向分组栏仍在滚动 / 布局时，首次打开 popover 会拿到未稳定锚点位置，需要打开后重定位。
- SwiftUI transient popover 与系统取色器焦点切换冲突，导致弹窗和取色器被关闭；已改为 AppKit 持久 popover 行为。
- 新建分组后顶部横向分组栏布局 / 滚动未稳定时打开 popover，会出现首次定位偏移；已增加重定位机制。

## 明日待办

- 继续手动验证主窗口和设置页两个入口的颜色与图标弹窗。
- 继续关注默认色板、取色器位置、图标选中态和分组重命名回归。

## 决策记录

- “颜色与图标”弹窗必须在取色器打开时保持打开，不能因系统取色器抢焦点自动关闭。
- 图标网格选中态统一为蓝底白图标。
