# 2026-05-21 开发日志

## 今日目标

- 收口文件预览交互问题，明确 Office / 表格类文件在统一预览窗口内的能力边界。
- 补充当前稳定性验收清单，作为继续性能和动画优化前的人工验收依据。

## 已完成

- 修复 Office 文件预览崩溃风险：移除嵌入式 Quick Look 的私有交互调用，避免 `QLPreviewView activate` 崩溃。
- 纠偏 Office 预览方案：放弃“底部可复制文本区”和“Office 转纯文本预览”，恢复以 Quick Look 保留原始格式作为主预览路径。
- 扩展保留格式预览范围：`doc`、`docx`、`docm`、`xls`、`xlsx`、`xlsm`、`xlsb`、`ppt`、`pptx`、`pptm`、`csv`、`rtf`、`numbers`、`pages`、`key` 等走嵌入式 Quick Look。
- 保留普通文本预览范围：`txt`、`md`、`json`、`xml`、`log`、源码文件等仍使用可选择文本预览。
- 新增 `docs/V2_CURRENT_STABILITY_CHECKLIST.md`，记录当前版本人工验收清单和已知边界。
- 更新 `docs/KNOWN_ISSUES.md`，记录嵌入式 Quick Look 在保留格式时无法稳定保证 Office 内容选择 / 复制。
- 更新 `docs/V2_OPTIMIZATION_BACKLOG.md`，将“保留格式 + 统一预览窗口 + Office 内容完全可选择复制”的冲突归档为后续专项。

## 验证结果

- 已执行 `./scripts/build-app.sh --bump patch --run`，最终运行版本为 `2.0.49 (260521.1555)`。
- App 进程运行正常，记录到的 PID 为 `56423`。
- 崩溃日志无新增，最新 ClipEase 崩溃报告仍停留在 `2026-05-21 04:53` 的旧崩溃。
- 代码路径确认 Office / CSV / RTF / iWork 格式回到 `HistoryFileQuickLookPreviewView`，不再走纯文本提取。
- Computer Use 对当前菜单栏窗口多次返回 timeout，未能完成 UI 自动化点击验证；此项需要用户人工验收。

## 已知边界

- Finder 空格预览可选择部分 Office 内容，但轻贴统一预览窗口内嵌的公开 `QLPreviewView` 不能稳定强制提供相同选择 / 复制能力。
- 私有 Quick Look 激活接口已确认会导致崩溃，不再使用。
- 若未来必须同时满足“保留原格式、统一预览窗口、Office 内容全量可选择复制”，需要另开专项评估 HTML / PDF 转换或文档渲染引擎。

## 下一步

- 由用户按 `docs/V2_CURRENT_STABILITY_CHECKLIST.md` 做当前版本人工验收。
- 验收无阻塞后，继续推进性能和动画优化：主窗口不卡顿、卡片拖拽动画、选中聚焦、后台资源预热等。

## 后续推进记录

- 用户确认当前稳定性人工验收无阻塞。
- 进入性能 / 动画优化下一批，优先收口后台资源预热规则。
- 将历史卡片资源预热常量从 `preheatItemLimit` 改名为 `preheatBatchSize`，明确含义为“每批大小”，不是只处理前 18 个。
- 新增 `scripts/verify_history_preheat_all_items.py`，静态守卫后台预热必须遍历全部 `filteredItems`，禁止回退成 `prefix(18)` 或只预热第一批。
- 继续收口拖拽视觉：卡片拖拽快照保持整张卡片 `0.82` 缩放，并以 `0.82` 透明度绘制，拖拽浮层继续以鼠标中心定位并带系统阴影。
- 新增 `scripts/verify_card_drag_visuals.py`，守卫拖拽快照缩放、半透明、阴影、鼠标中心定位和拖动前选中聚焦路径。
- 收口主窗口打开 / 关闭动画性能守卫：确认窗口只做轻量 frame 位移动画，不引入 alpha 动画、内容层 transform 或重阴影。
- 新增 `scripts/verify_history_window_animation_performance.py`，守卫显示完成后再安装外部点击监听、预览内容交互时不被主窗口外点逻辑误关、隐藏时取消后台任务。
- 已执行 `swift build`、`./scripts/build-app.sh --bump patch --run`，当前运行版本为 `2.0.50 (260521.1620)`，PID `66406`。
- 收口搜索 / 筛选性能守卫：确认搜索输入走防抖、取消旧任务、后台过滤、取消传播和 generation 防旧结果覆盖，主线程只应用最新过滤结果。
- 新增 `scripts/verify_history_search_performance_guards.py`，守卫大历史量搜索路径不能回退成主线程同步过滤，也不能绕过 normalized search text 缓存。
- 已执行 `swift build`、`./scripts/build-app.sh --bump patch --run`，当前运行版本为 `2.0.51 (260521.1641)`，PID `78786`。
- 新增链接卡片预览图元数据能力：复制 URL 后后台抓取网页标题、`og:image` / `twitter:image`，并补充 `fluid-icon` / `apple-touch-icon` / `icon` 站点图标兜底，用现有图片附件路径保存预览图。
- 链接历史卡片中间区域优先显示已保存的预览图；没有图片元数据或下载失败时继续回退到原来的渐变 link 图标，不影响旧链接卡片。
- 更新 `scripts/verify_link_title_background_fetch.py` 为链接元数据守卫，覆盖后台抓取、陈旧 URL 防护、图片保存、旧图片清理和卡片图片渲染路径。
- 已执行 `swift build`、`python3 scripts/verify_link_title_background_fetch.py`；编译通过，仍存在既有 `ClipboardOCRService.swift` Vision Sendable 警告。
- 按运行要求执行 `./scripts/build-app.sh --bump patch --run`，旧进程 `78786` 已结束，新版本 `2.0.52 (260521.1656)` 已启动，当前 PID `90682`。
- 修复“复制新链接但经常不触发链接元数据抓取”：RTF / HTML 富文本剪贴板分支现在会先判断 `plainText` 是否是完整 URL，是 URL 时走 `addText` 链接路径，普通富文本仍走 `addRichText` 保留格式。
- 新增 `scripts/verify_rich_text_url_link_capture.py`，守卫 RTF / HTML 两条分支都不能绕过 URL plain-text 捕获路径；已执行该脚本、`python3 scripts/verify_link_title_background_fetch.py` 和 `swift build`。
- 按运行要求执行 `./scripts/build-app.sh --bump patch --run`，新版本 `2.0.53 (260521.1808)` 已启动，当前 PID `23734`。
- 调整链接卡片预览图渲染：链接元数据图片现在使用“不放大小图”的模式，最大 96pt 且不超过原始图像尺寸，避免 favicon / 小站点图标被强制放大后模糊；普通图片卡片仍保持铺满可用区域。
- 更新 `scripts/verify_link_title_background_fetch.py`，守卫链接预览图不能回退到强制放大路径；已执行该脚本和 `swift build`。
- 按运行要求执行 `./scripts/build-app.sh --bump patch --run`，新版本 `2.0.54 (260521.1829)` 已启动，当前 PID `32507`。
- 调整链接预览图尺寸下限：链接元数据图片现在保持在 `52pt...96pt` 区间，小站点图标最小与默认链接图标块一致，避免过小；继续限制最大尺寸，避免大幅放大导致模糊。
- 已执行 `python3 scripts/verify_link_title_background_fetch.py` 和 `swift build`。
- 按运行要求执行 `./scripts/build-app.sh --bump patch --run`，新版本 `2.0.55 (260521.1834)` 已启动，当前 PID `35881`。
- 优化链接元数据抓取速度：HTML 抓取完成后先回写标题，不再等待预览图下载；预览图复用同一次 HTML 结果继续后台下载并单独回写，避免图片慢拖住整张卡片更新。
- 修复链接详情预览空白反馈：`LinkPreviewWebView` 增加 `WKNavigationDelegate`，详情预览现在显示加载中状态和失败提示，避免可打开链接在内嵌预览失败时呈现纯白无反馈。
- 更新 `scripts/verify_link_title_background_fetch.py`，守卫标题先更新、图片复用 HTML、不重复抓 HTML，以及链接预览加载 / 失败状态；已执行该脚本和 `swift build`。
- 按运行要求执行 `./scripts/build-app.sh --bump patch --run`，新版本 `2.0.56 (260521.1840)` 已启动，当前 PID `39605`。
- 增加链接元数据后台并发限流：`fetchLinkMetadata` 继续在 `Task.detached(priority: .utility)` 执行，但通过 `LinkMetadataFetchLimiter` 将同时进行的网络 / 图片解码 / 图片保存任务限制为 3 个，避免批量链接抓取挤占系统资源导致主窗口卡顿。
- 链接元数据后台任务在标题回写和图片下载之间显式 `Task.yield()`；主线程仍只做短状态合并，不执行网络、HTML 解析、图片解码或磁盘写入。
- 更新 `scripts/verify_link_title_background_fetch.py`，守卫链接抓取必须使用后台并发限流和阶段 yield；已执行该脚本和 `swift build`。
- 按运行要求执行 `./scripts/build-app.sh --bump patch --run`，新版本 `2.0.57 (260521.1848)` 已启动，当前 PID `42528`。
- 收口链接元数据后台任务生命周期：同一链接新抓取会取消旧任务，删除卡片、清空历史、删除分组、重复项替换和过期清理都会取消对应抓取任务，避免无用网络 / 图片处理继续占用资源。
- 更新 `scripts/verify_link_title_background_fetch.py`，守卫链接元数据任务表、generation 清理、取消路径和限流槽释放；已执行链接守卫、搜索 / 窗口 / 点击 / 富文本 URL 守卫和 `swift build`。
- 按运行要求执行 `./scripts/build-app.sh --bump patch --run`，新版本 `2.0.58 (260521.1911)` 已启动，当前 PID `54552`。
- 收口 OCR 动态并发：空闲状态最多 5 个后台 OCR 任务，主窗口 / 预览交互期间降到 2 个；OCR 任务仍保持 utility 优先级，主线程只做状态和结果写回。
- 补齐 OCR 生命周期清理：删除卡片、清空历史、删除分组、重复项替换和过期清理都会取消对应 OCR 任务；已完成的 OCR 结果继续持久化在 `ClipboardItem`，打开主窗口不会重复识别。
- 新增 `scripts/verify_ocr_background_lifecycle.py`，守卫 OCR 动态并发、可取消路径、生命周期清理和持久化结果；已执行 OCR / 链接 / 搜索 / 窗口 / 点击守卫和 `swift build`。
- 按运行要求执行 `./scripts/build-app.sh --bump patch --run`，新版本 `2.0.59 (260521.2009)` 已启动，当前 PID `81568`。
- 收口预览窗口打开 / 关闭性能：文本和颜色继续即时加载，图片、链接、文件等重内容延后到轻量预览 shell 显示后再加载，避免打开动画阶段被磁盘 IO、图片解码、Web 预览或文件预览初始化挤占。
- PDF 预览从同步 `PDFDocument(url:)` 改为可取消的 utility 任务加载；切换文件时先卸载旧 document，关闭预览时取消待加载任务并先清空 heavy content，减少关闭动画期间的重绘和资源占用。
- 新增 `scripts/verify_preview_window_performance_guards.py`，守卫预览窗口重内容延后加载、图片后台 IO、PDF 可取消加载、关闭前卸载内容，以及链接预览加载 / 失败状态。
- 已执行预览 / OCR / 链接 / 搜索 / 窗口动画 / 卡片点击守卫、`python3 scripts/smoke_check.py` 和 `swift build`；编译通过，仍存在既有 Vision / PDFKit Sendable 警告。
- 按运行要求执行 `./scripts/build-app.sh --bump patch --run`，新版本 `2.0.60 (260521.2051)` 已启动，当前 PID `2304`。
- 暂缓性能推进，先接入轻贴复制 / 粘贴音效：将 `/Applications/Paste.app/Contents/Resources/Copy.aiff` 和 `Paste.aiff` 复制到项目 `Resources/Sounds/`，并让 `scripts/build-app.sh` 打包到 `.app/Contents/Resources/Sounds/`。
- 新增 `ClipEaseSoundPlayer`，从 bundle 内 `Sounds/Copy.aiff` 和 `Sounds/Paste.aiff` 加载音效；复制成功播放 Copy，真正自动粘贴发出 `Command+V` 后播放 Paste，授权不足只写入剪贴板时按复制语义播放 Copy。
- 接入主窗口复制、纯文本复制、链接 / 颜色 / 图片 / 文件 / 路径直接复制、预览窗口复制和 OCR badge 复制路径；失败路径不播放音效。
- 新增 `scripts/verify_sound_feedback_guards.py`，守卫声音资源必须在项目内、构建脚本必须复制资源、复制 / 粘贴路径必须触发对应反馈。
- 已执行 `python3 scripts/verify_sound_feedback_guards.py`、`swift build` 和 `./scripts/build-app.sh --bump patch --run`；新版本 `2.0.61 (260521.2114)` 已启动，当前 PID `13931`，bundle 已确认包含 `Sounds/Copy.aiff` 与 `Sounds/Paste.aiff`。
- 用户回报“只有粘贴有音效，复制没有音效”，按这个现象继续收口：复制反馈后端从 `NSSound` 改为 `AVAudioPlayer` 并提前 `prepareToPlay()`，避免短音效首次播放时被系统延迟或资源状态影响。
- 已将 RC / 稳定性清单同步到 `2.0.62 (260521.2144)`，并重新执行 smoke、音效守卫、`swift build` 和 build/run；当前运行 PID 为 `27503`，bundle 内资源仍在 `Sounds/Copy.aiff` 和 `Sounds/Paste.aiff`。
- 纠正复制音效 bug 的理解范围：用户反馈的是“在其他 App 复制内容后，轻贴捕获新增卡片没有 Copy 音效”，不是轻贴内部复制按钮。已在 `ClipboardHistoryStore` 成功 upsert 外部来源卡片后播放 Copy 音效，覆盖文本、富文本、图片和文件捕获；`item.isFromClipEase` 会排除轻贴自身写入剪贴板的卡片，避免重复播放。
- 扩展 `scripts/verify_sound_feedback_guards.py`，守卫外部捕获成功后必须播放 Copy，并且必须过滤轻贴自身来源。
- 已执行 `python3 scripts/verify_sound_feedback_guards.py`、`swift build` 和 `./scripts/build-app.sh --bump patch --run`；新版本 `2.0.63 (260521.2249)` 已启动，当前 PID `50451`。
- 继续性能 / 动画优化：主窗口 `onAppear` 只保留必要状态恢复和最新卡片焦点准备，完整预览列表重建、搜索刷新和权限刷新延后到首帧后执行，降低窗口刚弹出时的主线程压力。
- 后台资源预热延迟从 160ms 调整到 260ms，继续按全量分批、低优先级、可取消方式处理全部卡片资源，避免与打开动画和新增卡片定位抢资源。
- 更新 `scripts/verify_history_window_animation_performance.py`，守卫 deferred startup、预览列表延后重建、权限刷新延后和更保守的预热延迟。
- 已执行主窗口动画守卫、音效守卫、`swift build` 和 `./scripts/build-app.sh --bump patch --run`；新版本 `2.0.64 (260521.2338)` 已启动，当前 PID `67098`。
