# 2026-05-24 开发日志

## 00:00 开发测试数据批量插入修复

### 用户指令

- 用户询问开发入口生成 10,000 条测试数据是否有问题，以及为什么 1 万条会导致数据库很大。
- 确认后要求修复。

### 根因

- 当前真实库 `/Users/wpc/Library/Application Support/ClipEase/ClipEase.sqlite` 仅约 `372KB`，运行前为 `76` 条记录，FTS 也是 `76` 行；本轮验证未污染真实库。
- 设置页“生成 10,000 条”入口调用 `store.addDebugTextItems(count: 10_000)`。
- 旧实现会先在内存插入全部 debug item，然后 `mergeDebugTextItems()` 调用 `scheduleSave()`，进入全量 snapshot 保存。
- 全量保存最终走 `SQLiteClipboardStore.replaceAllItems()`，会删除所有表后逐条重插所有历史，并为每条写 FTS；这会放大耗时和 WAL/FTS 体积，不适合作为开发压测入口。
- 数据库变大的合理来源包括：debug 文本本身、主表字段、content_hash/index、FTS5 的 `_data` / `_content` 内部表，以及 WAL 未 checkpoint 时的 `ClipEase.sqlite-wal`。

### 实现内容

- `ClipboardHistoryRepository` 新增 `insertItems(_:)`。
- `ClipboardHistoryPersistence` 新增 `insertItemsOrThrow(_:)`。
- `SQLiteClipboardStore.insertItems(_:)` 使用单个 `BEGIN IMMEDIATE TRANSACTION` 批量插入 item、group item 和 FTS，不再删除重写整库；提交后执行 `PRAGMA wal_checkpoint(PASSIVE)`，让开发者观察到的数据库体积更接近真实主库体积。
- `ClipboardHistoryStore.mergeDebugTextItems()` 不再调用 `scheduleSave()`，改为 `persistDebugItemsIncrementally(newItems)`。
- `ClipboardHistorySaveWriter` 新增 `insertItemsAsync(_:revision:)`，保持写库串行队列，不阻塞主线程。
- 新增性能埋点：
  - `history.store.addDebugTextItems`
  - `history.persistence.insertDebugItems`
- 新增守卫脚本 `scripts/verify_debug_data_bulk_insert_guards.py`，禁止 debug 数据生成回退到 `scheduleSave()` / 全量保存路径。

### 行为影响

- 只影响设置页开发测试数据生成入口。
- 普通剪贴板新增、删除、搜索、滚动、预览逻辑不变。
- 测试数据仍会出现在 UI 顶部，并保留现有排序语义。

### 验证

- `python3 scripts/verify_debug_data_bulk_insert_guards.py` 先失败后通过。
- 全量性能守卫通过：
  - `verify_sqlite_fts_search_guards.py`
  - `verify_latest_card_left_padding_guards.py`
  - `verify_startup_paged_history_load_guards.py`
  - `verify_sqlite_incremental_delete_guards.py`
  - `verify_sqlite_incremental_upsert_guards.py`
  - `verify_store_index_performance_guards.py`
  - `verify_history_visible_window_range_guards.py`
  - `verify_history_unfiltered_search_fast_path_guards.py`
  - `verify_history_filtered_index_guards.py`
  - `verify_sqlite_snapshot_bulk_load_guards.py`
  - `verify_history_reopen_stability_guards.py`
  - `verify_history_panel_show_performance_guards.py`
  - `verify_history_diagnostics_throttle_guards.py`
- `swift build` 通过。
- `swift test` 已执行；项目当前无 Tests target，输出 `error: no tests found`。
- `./scripts/build-app.sh --bump patch --run` 通过，最终运行 `2.3.39 (260523.2355)`，PID `25280`。
- 最新日志：`/Users/wpc/Library/Application Support/ClipEase/PerformanceLogs/2026-05-23_23-56-50.jsonl`。
- 运行指标：
  - `history.store.loadStartupPage`：`2.85ms`，首批 `76` 条。
  - `history.store.initialize`：`3.21ms`。
  - `history.store.searchIndexWarmup`：`2.08ms`。
  - 稳定后主线程采样约 `0.02ms - 0.10ms`。
- 数据库体积验证：
  - `ClipEase.sqlite`：约 `372KB`。
  - `clipboard_items`：`76` 行。
  - `clipboard_items_fts`：`76` 行。

## 00:14 预览窗口顶部拖动抖动修复

### 用户指令

- 用户已加入 10 万条级别数据，性能不卡顿。
- 反馈预览窗口顶部按住拖动时窗口会抖动。

### 根因

- 预览顶部 `HeaderDragView` 在 `mouseDragged` 超过阈值后，先调用 `onDragStarted()` 将贴附预览转换成独立预览，并立即调整 panel frame。
- 随后旧实现仍用最初的 `mouseDown` 事件调用 `performDrag(with:)`。
- 这个 mouseDown 事件的窗口坐标基于 detach 前的 frame；detach 后 panel frame 已变化，系统拖动会用旧起点重新校正窗口位置，容易出现抖动。

### 实现内容

- 新增 `PreviewHeaderDragCompletion = (NSEvent) -> Void`。
- `HeaderDragView` 不再保存和复用初始 mouseDown event，只保存初始位置用于判断拖动阈值。
- 拖动真正开始时，`HeaderDragView` 将当前 `mouseDragged` event 传给 detach completion。
- `HistoryPreviewWindowController.detachCurrentPreview()` 返回接收当前 event 的 completion；detach 完成后由 controller 调用 `dragPanel.performDrag(with: event)`。
- 新增守卫脚本 `scripts/verify_preview_header_drag_guards.py`，禁止回退到“detach 后复用旧 mouseDown event 拖动”的路径。

### 行为影响

- 只影响预览窗口顶部拖动变成独立窗口的手势路径。
- 预览内容、复制、关闭、打开、搜索、列表滚动和普通窗口移动不变。

### 验证

- `python3 scripts/verify_preview_header_drag_guards.py` 先失败后通过。
- 全量守卫通过。
- `swift build` 通过。
- `swift test` 已执行；项目当前无 Tests target，输出 `error: no tests found`。
- `./scripts/build-app.sh --bump patch --run` 通过，最终运行 `2.3.40 (260524.0011)`，PID `35489`。
- 最新日志：`/Users/wpc/Library/Application Support/ClipEase/PerformanceLogs/2026-05-24_00-11-58.jsonl`。
- 当前数据量：`clipboard_items=70076`，`clipboard_items_fts=70076`，`ClipEase.sqlite` 约 `59MB`。
- 运行指标：
  - `history.store.loadStartupPage`：`27.53ms`，首批 `1000` 条。
  - `history.store.initialize`：`29.02ms`。
  - `preview.rebuild.background`：`22.41ms`，`2000` 条。
  - `history.store.searchIndexWarmup`：`120.99ms`。
  - 稳定后主线程采样约 `0.03ms - 0.17ms`。
- 构建残余警告仍为既有 Vision/PDFKit Sendable warning，本轮未改这些路径。

## 01:21 关于页内嵌赞赏二维码

### 用户指令

- 用户要求将赞赏页面直接改到关于页面空白位置中。
- 进一步明确：只保留“加入交流群”按钮跳转 GitHub 链接；移除赞赏按钮；在关于页面空白位置加入原来的支付宝和微信二维码，并保留原来的边框和内描边。

### 实现内容

- 移除 `SupportQRCodeSheet` 独立弹窗和 `isSupportSheetPresented` 状态。
- 关于页“支持与交流”区域只保留“加入交流群”按钮，继续走 `openSupportCommunity()`。
- 将支付宝和微信二维码直接嵌入 `aboutSection` 下方空白位置。
- 二维码继续使用原来的裁剪参数、白底、8pt 圆角、彩色 `strokeBorder(borderColor, lineWidth: 4)` 边框，保留原视觉边框 / 内描边效果。
- 新增 `scripts/verify_about_support_inline_guards.py`，防止赞赏按钮 / sheet 回归，并守卫关于页内嵌二维码。

### 行为影响

- 关于页不再弹出“赞赏支持”窗口。
- 用户仍可在关于页点击“加入交流群”。
- 赞赏二维码改为直接展示在关于页，减少一次点击。

### 验证

- `python3 scripts/verify_about_support_inline_guards.py` 先失败后通过。
- 全量守卫 `for script in scripts/verify_*guards.py; do python3 "$script"; done` 通过。
- `swift build` 通过。
- `swift test` 已执行；项目当前无 Tests target，输出 `error: no tests found`。
- `./scripts/build-app.sh --bump patch --run` 通过。
- 最终运行包：`2.3.43 (260524.0121)`，PID `79174`。
- 最新日志：`/Users/wpc/Library/Application Support/ClipEase/PerformanceLogs/2026-05-24_01-22-14.jsonl`。
- 运行指标：
  - `history.store.loadStartupPage`：`3.71ms`，首批 `1` 条。
  - `history.store.initialize`：`3.99ms`。
  - `preview.rebuild.background`：`0.08ms`。
  - `search.applyResults`：`0.01ms`。
  - `history.store.searchIndexWarmup`：`5.66ms`。
  - 稳定后主线程采样约 `0.02ms - 0.06ms`。
- Computer Use 读取 ClipEase 窗口超时，System Events 未返回窗口名；本轮以静态 UI 守卫、Swift 编译和 App 运行作为验证依据。
- 构建残余警告仍为既有 Vision/PDFKit Sendable warning，本轮未改这些路径。

## 00:50 清空历史重建数据库并删除历史缓存

### 用户指令

- 用户要求“清空数据库，也要把各类缓存和历史文件也都要删除”。

### 根因

- 旧 `SQLiteClipboardStore.deleteAllItemsAndGroups()` 使用事务逐表 `DELETE FROM` 清空记录。
- SQLite 主文件不会因为删除行自动缩小，FTS5 内部表和 freelist 仍保留在 `ClipEase.sqlite` 内。
- 本机实测在用户清空后仍为 `clipboard_items=1`、`ClipEase.sqlite` 约 `60MB`、`freelist_count=12293`，说明数据行少但数据库文件没有重建。
- Store 层 `deleteExternalFiles(for: removedItems)` 只能删除当前内存页已加载记录引用的附件；10 万+ 分页后无法覆盖未加载历史的图片、缩略图和富文本文件。

### 实现内容

- `SQLiteClipboardStore.deleteAllItemsAndGroups()` 改为：
  - 删除 `ClipEase.sqlite`、`ClipEase.sqlite-wal`、`ClipEase.sqlite-shm`、`ClipEase.sqlite-journal`。
  - 删除历史内容 / 缓存目录：`Images`、`Thumbnails`、`RichTexts`、`AppIcons`。
  - 重新调用 `initialize()` 创建空 schema。
- 为避免备份 / 导入等临时 SQLite 路径误删真实用户缓存，目录清理仅在当前 `databaseURL` 等于 `ClipEaseStoragePaths.sqliteStoreURL()` 时执行。
- 保留 `PerformanceLogs`，因为它是性能诊断依据，不属于剪贴板历史内容缓存。
- 新增 `scripts/verify_clear_history_recreates_database_guards.py`，防止清空历史回退到逐表 DELETE 或漏删历史缓存目录。
- 顺带恢复启动时音效预加载：`ClipEaseSoundPlayer.preloadFeedbackSounds()` 在剪贴板轮询启动前加载 Copy/Paste 播放器，避免第一次复制 / 粘贴时同步初始化音频。
- 更新既有守卫，使其匹配当前已落地实现：
  - `verify_card_click_performance_guards.py` 对齐当前 `itemIDsByHash` 缓存去重写法。
  - `verify_context_menu_editor_color_guards.py` 对齐当前富文本颜色降级为文本后由 Store 识别颜色卡片的实现。
  - `verify_preview_window_performance_guards.py` 对齐预览标题栏拖动使用当前 `mouseDragged` event 的实现。

### 行为影响

- 用户点击“清空历史”后，会清空并重建历史数据库，同时删除历史图片、缩略图、富文本附件和来源 App 图标缓存。
- 首次清空后来源 App 图标会按后续新记录重新生成；这是缓存重建，不改变剪贴板功能。
- 性能日志不会被清空，方便继续分析卡顿和启动指标。
- 普通新增、删除、搜索、滚动、预览、备份导入和 UI 布局不改变。

### 验证

- `python3 scripts/verify_clear_history_recreates_database_guards.py` 先失败后通过。
- 全量守卫 `for script in scripts/verify_*guards.py; do python3 "$script"; done` 通过。
- `swift build` 通过。
- `swift test` 已执行；项目当前无 Tests target，输出 `error: no tests found`。
- `./scripts/build-app.sh --bump patch --run` 通过，随后 `./scripts/build-app.sh --bump none --run` 通过。
- 最终运行包：`2.3.41 (260524.0049)`，PID `57700`。
- 最新日志：`/Users/wpc/Library/Application Support/ClipEase/PerformanceLogs/2026-05-24_00-50-17.jsonl`。
- 运行指标：
  - `history.store.loadStartupPage`：`1.49ms`，首批 `1` 条。
  - `history.store.initialize`：`1.76ms`。
  - `preview.rebuild.background`：`0.07ms`。
  - `search.applyResults`：`0.01ms`。
  - `history.store.searchIndexWarmup`：`1.89ms`。
  - 稳定后主线程采样约 `0.02ms - 0.05ms`。
- 当前未直接替用户执行真实“清空历史”动作；真实库仍是此前已清空后的旧文件状态，`ClipEase.sqlite` 约 `60MB`、`clipboard_items=1`、`freelist_count=12293`。需要在新版本 App 内再次点击“清空历史”后，才会走本轮新的删除数据库 / 删除缓存 / 重建 schema 路径。
- 构建残余警告仍为既有 Vision/PDFKit Sendable warning，本轮未改这些路径。

## 01:06 右键复制来源 App 名称重复新增修复

### 用户指令

- 用户反馈在 Codex 卡片中点击“复制来源 App 名称”会出现 2 个新增卡片：一个来自轻贴，一个来自 Codex。
- 用户询问应该保留哪张卡片更合理。

### 判断

- 应保留“来自轻贴”的卡片。
- 该内容是用户在轻贴内部执行命令后主动写入剪贴板的结果，来源语义应是轻贴。
- “来自 Codex”的第二张是剪贴板监听器捕获同一次 pasteboard 写入后再次入库，属于重复记录。

### 根因

- `copySourceAppName(_:)` 手动调用 `NSPasteboard.general.setString(...)`，随后通过 `addClipEaseTextCard(...)` 插入一条来源为 `.clipease` 的历史。
- 剪贴板监听器稍后看到同一次系统剪贴板变化，又以当前前台 App 作为来源调用 `store.addText(...)`，于是生成第二条来源为 Codex 的卡片。
- Store 已有 `skipNextClipboardText(_:)` 机制，但右键菜单的手动文本复制路径没有调用它。

### 实现内容

- `HistoryWindowView.addClipEaseTextCard(_:)` 在 `store.addText(..., sourceApp: .clipease)` 前调用 `store.skipNextClipboardText(text)`。
- `HistoryWindowController.copyPlainPreviewText(_:)` 和 `copyPreviewFilePaths(for:)` 同步补上 `skipNextClipboardText`，覆盖预览窗口内同类手动文本复制路径。
- 新增 `scripts/verify_internal_copy_skip_guards.py`，防止内部文本复制路径忘记跳过下一次剪贴板监听。

### 行为影响

- “复制来源 App 名称 / Bundle ID / 链接 / 路径 / 预览文本”等轻贴内部文本复制动作只保留 1 张来源为轻贴的历史卡片。
- 外部 App 的真实复制行为不受影响，仍按外部 App 来源记录。

### 验证

- `python3 scripts/verify_internal_copy_skip_guards.py` 先失败后通过。
- 全量守卫 `for script in scripts/verify_*guards.py; do python3 "$script"; done` 通过。
- `swift build` 通过。
- `swift test` 已执行；项目当前无 Tests target，输出 `error: no tests found`。
- `./scripts/build-app.sh --bump patch --run` 通过。
- 最终运行包：`2.3.42 (260524.0105)`，PID `67784`。
- 最新日志：`/Users/wpc/Library/Application Support/ClipEase/PerformanceLogs/2026-05-24_01-06-16.jsonl`。
- 运行指标：
  - `history.store.loadStartupPage`：`4.21ms`，首批 `4` 条。
  - `history.store.initialize`：`4.47ms`。
  - `preview.rebuild.background`：`0.17ms`。
  - `search.applyResults`：`0.01ms`。
  - `history.store.searchIndexWarmup`：`4.38ms`。
  - 稳定后主线程采样约 `0.03ms - 0.05ms`。
- 构建残余警告仍为既有 Vision/PDFKit Sendable warning，本轮未改这些路径。
