## 今日完成

- 修复设置窗口关闭行为：红色关闭按钮和 `Command + W` 改为隐藏设置窗口，不再真正关闭窗口对象，避免触发 App 退出。
- 新增统一辅助功能权限状态 `AccessibilityPermissionState`，历史窗口和设置页共享同一权限状态。
- App 激活、设置页显示、历史窗口显示和手动刷新时会重新读取辅助功能权限。
- 设置页在权限异常时提示用户确认授权的是当前运行的 `ClipEase.app`。
- 权限页新增当前运行 App 路径显示。
- 权限页新增“显示当前 App”和“复制 App 路径”，用于排查辅助功能授权对象不一致。
- 粘贴执行链路新增明确结果：已粘贴、仅复制和失败原因。
- 图片文件缺失、图片无法读取或剪贴板写入失败时会显示具体提示，不再静默失败。
- 历史窗口粘贴/复制状态提示按文本、富文本、链接、图片和颜色区分。
- 修复自动粘贴焦点问题：历史窗口打开前记录原前台 App，自动粘贴前关闭历史窗口并重新激活原 App，再发送 `Command + V`。
- 二次修复自动粘贴焦点问题：自动粘贴前改为立即隐藏历史窗口，并将 `Command + V` 直接投递到原目标 App 进程。
- 按 Paste 的交互方式修正自动粘贴策略：底部历史窗口改为非激活面板显示，不激活轻贴、不抢走原 App 焦点；自动粘贴恢复为系统级 `Command + V`。
- 新增历史窗口全局键盘事件路由：底部窗口非激活显示时，仍可使用左右键、回车、空格、Esc、`Command + 1-9`、`Command + F/C/P` 操作。
- 键盘事件路由只在历史窗口显示期间安装，关闭或自动粘贴前立即卸载，避免影响普通输入。
- `Command + 1-9` 序号显示改为由非激活窗口事件状态驱动，保持不抢焦点时也能显示卡片序号。
- 修正搜索输入模式：历史窗口改为可成为键盘窗口的非激活面板，轻贴不激活为前台 App，也不主动切走原 App。
- 搜索、预览、卡片选择和快捷键操作都在非激活面板内完成；双击或回车粘贴时仍通过系统级 `Command + V` 落到原 App 光标位置。
- 完善搜索结果体验：搜索内容变化后自动选中第一条结果，搜索为空结果时关闭预览并阻止回车粘贴。
- 清空搜索后恢复到合理选中项，预览打开时会同步刷新预览内容。
- `Esc` 在搜索展开时先退出搜索，搜索未展开时再关闭历史窗口。
- 新增输入即搜索：历史窗口打开后直接输入可打印字符，会自动展开搜索框并把输入内容写入搜索。
- 输入即搜索保留方向键、回车、空格、Esc 和 Command 快捷键原有行为；搜索框聚焦后后续输入交给原生文本框处理，避免重复输入。
- 修正搜索 Esc 状态机：搜索有内容时先清空内容，搜索框为空时再关闭搜索框，搜索框关闭后再关闭轻贴。
- 搜索框清空按钮改为只清空内容，不再直接关闭搜索框。
- 修复输入即搜索后的选区问题：自动展开搜索框后光标停在文本末尾，不再默认选中首个字符。
- 搜索框聚焦时 Esc 仍由轻贴接管，按清空内容、关闭搜索框、关闭轻贴的顺序执行。
- 搜索框聚焦时 Delete 和 `Command + A` 回到原生文本框处理，支持回退删除和全选。
- 加强搜索框焦点：输入即搜索展开后显式让原生搜索框成为第一响应者并显示光标，避免搜索框无光标、无法回退删除或无法使用原生编辑快捷键。
- 面板级 Esc 改为派发到统一搜索状态机，避免绕过搜索清空流程直接关闭轻贴。
- 搜索框聚焦时除 Esc 和 `Command + 1-9` 外，其余编辑快捷键交给原生文本框处理。
- 搜索框光标在末尾且存在结果时，按右键、下键或回车会进入第一张结果卡片；没有结果时保留在搜索框。
- 从搜索框末尾再次输入字符会替换原搜索内容并开始新搜索；搜索框未关闭时保留搜索状态，不在粘贴时自动清空。
- 修正搜索框展开期间快捷键规则：搜索框展开或聚焦时，事件层不再拦截 `Command + 1-9`、Esc 和其他快捷键，全部优先交给搜索框/系统原生处理。
- 搜索框内 Esc 通过原生文本框取消回调进入轻贴搜索状态机，仍按清空内容、关闭搜索框、关闭轻贴的顺序执行。
- 重新对齐搜索快捷键规则：搜索框聚焦时只特殊接管 Esc 和 `Command + 1-9`；其他快捷键走原生文本框。
- `Command + 1-9` 改为直接粘贴对应结果卡片，不再只是选中卡片；粘贴后沿用自动隐藏主窗口链路。
- 搜索框有内容但未聚焦时，Esc 按主窗口规则关闭轻贴；搜索框聚焦时 Esc 才按清空内容、关闭搜索框、关闭轻贴执行。
- 主窗口显示时新增外部点击关闭：点击非主窗口位置会关闭轻贴。
- 修正搜索框展开但未聚焦时的 Esc 顺序：先关闭搜索框，再由下一次 Esc 关闭主窗口。
- 优化搜索性能：历史窗口不再在每次输入时通过计算属性反复全量构造和过滤卡片。
- 新增搜索结果缓存状态，输入变化后使用可取消任务防抖，再在后台线程过滤结果，主线程只应用最新结果。
- 图片和 App 图标预热改为延迟任务，避免搜索输入时同步触发文件读取和缓存预热。
- 关闭历史窗口时取消未完成的搜索和预热任务，避免后台任务在窗口关闭后回写状态。
- 修复搜索框展开但未聚焦时的 Esc 顺序：先清空搜索内容并关闭搜索框，再由下一次 Esc 关闭主窗口。
- 修正搜索框聚焦快捷键规则：搜索框聚焦时只拦截 Esc 和 `Command + 1-9`，其余如 `Command + A/X/W` 等交给原生文本框处理。
- 主窗口新增快捷键：`Command + W` 关闭主窗口，`Command + N` 新建文本，`Command + T` 暂停或恢复记录。
- `Command + 1-9` 在任意状态下直接粘贴对应结果卡片，等同回车/双击粘贴。
- 历史窗口渲染上限改为首屏 14 张、展开后最多 40 张，避免搜索时大量卡片视图同时刷新导致掉帧。
- 撤销卡片渲染上限方案，恢复完整搜索结果数据，避免因性能优化导致数据不完整。
- 搜索框改为自定义原生 `NSTextField`，在非激活窗口内显式处理 `Command + A/C/X/V/Z/W`，确保系统文本快捷键可用。
- 搜索结果应用到界面时禁用隐式动画，减少搜索输入期间大量卡片刷新导致的掉帧。
- 移除历史 Store 的 80 条硬性内存上限，避免真实历史被自动截断删除。
- 新增开发测试数据入口：更多菜单可添加 1,000 或 10,000 条文本记录，用于验证大数据量搜索和滚动性能。
- 历史 Store 新增防抖后台写盘：普通新增、导入、置顶和测试数据批量添加会合并保存，降低连续写盘造成的卡顿。
- 删除、清空、保存期限清理和 App 退出前会立即刷新写盘，避免延迟保存导致历史索引与附件文件不一致。
- 开发测试菜单新增“清除测试文本”，可一键删除批量生成的性能测试文本，避免污染真实历史。
- 开发测试文本生成改为后台任务，主线程只在生成结束后合并一次结果，降低添加 10,000 条测试数据时的界面卡顿风险。
- 开发测试菜单新增当前测试文本数量显示，便于确认测试数据添加和清理状态。
- 历史窗口预览项和搜索索引构建改为后台任务，主线程只应用生成后的完整结果，降低大数据量历史刷新时的卡顿风险。
- 设置页历史数据清理新增“清理孤立附件”，可删除未被当前历史引用的图片、缩略图和富文本文件，并显示释放空间。
- 修正正式打包版本规则：`scripts/build-app.sh` 默认递增 patch 版本并更新时间戳，避免产品版本长期停留在 `0.1.0`。
- 明确版本递增规则：大版本 +1，小版本 +0.1，修补 +0.0.1；移除不更新时间戳的脚本参数，确保每次打包都会更新时间戳。
- 设置页存储用量计算和孤立附件清理改为后台任务，清理前增加确认弹窗，任务进行时禁用对应按钮。
- 设置页导入、导出和备份包操作失败时改为展示具体错误原因，并写入系统日志。
- 设置页导入历史、导出历史、导入备份包和导出备份包改为后台任务，操作期间显示处理中状态并禁用对应按钮。
- 备份包导入新增完整性校验与结果统计：缺少 `history.json` 时给出明确错误，导入后显示导入、跳过和缺失附件数量。
- 设置页新增只读数据健康检查，统计缺失图片、缺失富文本、孤立附件数量和占用空间。
- 数据健康检查完成后新增系统对话框，展示缺失图片、缺失富文本、孤立附件数量和占用空间详情。
- 新增第一版回归测试清单，覆盖启动、记录、卡片、搜索、预览、新建文本、设置页、数据维护、性能和发布前检查。
- README 更新为第一版稳定性收尾状态，并补充当前功能列表和验收清单入口。
- 修复第一版发布前卡片交互：点击卡片不再自动滚动居中，鼠标滚轮支持横向滚动。
- 修复 App 图标取色逻辑：从图标中心区域选择最深的非白/非灰颜色，不再额外加深。
- 修复 URL 识别误判，避免将 `PRODUCT_REQUIREMENTS.md` 这类文件名识别成链接。
- 修复 URL 卡片标题 fallback，根路径链接标题显示 `/`，副标题显示完整 URL。
- URL 预览新增明确“打开链接”操作，并支持点击预览内容打开链接。
- 修复预览打开时点击当前卡片不关闭的问题，点击任一卡片都会先关闭预览。
- 修复帮助窗口关闭导致轻贴进程退出的风险，关闭帮助页仅隐藏帮助窗口。
- 新建文本窗口支持用户调整大小，`Command + W` 只关闭编辑窗口；失焦不会隐藏窗口。
- 新建文本 B/I/U/S 增加按下状态背景，且无选区时只影响光标后的新输入内容，不再改动已有全文。
- 主窗口新增本 App 内部窗口点击监听，点击设置、帮助等非主窗口区域也会关闭主窗口。
- 复制、双击粘贴和 `Command + 1-9` 粘贴后，记录会移动到置顶内容后第一位。
- 新建文本改为支持多个独立窗口，任一窗口点击取消都会关闭自身窗口并释放对应控制器。
- 新建文本窗口补齐 `Command + A/C/X/V/Z/W` 等编辑快捷键，避免无标题栏面板吞掉系统快捷键。
- 链接识别改为只接受明确带 `http://` 或 `https://` 的内容，并支持 IPv4 地址链接。
- 链接卡片内容区不再重复展示 URL，网页标题获取失败时显示 `/`，URL 保留在卡片底部。
- 链接标题新增后台抓取 `<title>`，抓取失败时保持 `/`。
- 链接预览窗口改为内嵌网页加载当前 URL，并保留右下角外部打开按钮。
- 链接和图片预览窗口按屏幕宽度 50% 计算内容区尺寸，4K 屏内容区为 1920 × 1080，其他分辨率等比例缩放。
- 文本预览窗口按文本行宽和行高测量尺寸，并限制在当前屏幕等比例上限内；超出内容支持横向和纵向滚动。
- 图片预览改为滚动视图承载原图，窗口不足时可横向/纵向滚动查看。
- 修正大预览窗口定位：预览窗口始终贴在当前卡片上方，箭头保持指向当前卡片中心。
- 文本预览改为按最大宽度自动换行后测量高度，4K 下最高不超过 1080，超出部分用纵向滚轮查看。
- 优化预览窗口动画：打开时先渲染轻量占位窗口，动画完成后再延迟加载 WebView、图片或长文本内容。
- 优化预览关闭动画：关闭前先卸载重内容，再执行轻量淡出，减少关闭瞬间主线程布局压力。
- 图片预览文件读取改为后台读取 Data，主线程只负责创建显示对象，降低大图预览打开时的卡顿风险。
- 长文本预览尺寸计算改为轻量估算，避免打开预览前在主线程进行完整富文本排版测量。
- 图片预览超出窗口时改为等比例缩小适配，不再出现横向/纵向滚动条。
- 主窗口卡片列表滚轮改为本地事件监听转横向滚动，修复滚轮无法滚动卡片的问题。
- 预览打开/关闭动画时长调慢并使用 Paste 风格缓动，保持轻量窗口先动、重内容后加载。
- 主窗口滚轮逻辑改为在窗口视图树中查找鼠标下方可横向滚动的 `NSScrollView`，不再依赖嵌入视图的 `enclosingScrollView`。
- 图片预览改为在预览内容区内等比例缩放铺满可用区域，不覆盖顶部标题栏和底部信息栏。
- 文本预览改为按固定内容区宽度自动换行，并分块逐帧追加长文本，避免一次性加载造成明显迟缓。
- Esc 快捷键改为预览优先：预览打开时先关闭预览，再次 Esc 才继续关闭主窗口。
- 主窗口滚轮转换增加速度倍率，区分精确触控板与普通鼠标滚轮，提升横向滚动速度。
- 点击当前视图左右边缘未完整显示的卡片时，自动将卡片滚到刚好完整显示的位置。
- 图片预览窗口根据图片宽高比计算内容区高度，宽图铺满最大宽度时窗口高度随图片高度收缩。
- 图片预览去除内容区内边距，图片按比例最大化占满预览窗口内容区。
- 主窗口滚轮方向修正为向下滚动查看右侧卡片，并继续提高滚轮横向滚动速度。
- 图片预览窗口尺寸按缩放后的图片内容高度取整计算，去除上下残留间隙。
- 边缘半露卡片点击改为底层横向偏移滚动，当前卡片完整显示并额外露出下一张约 1/6，附带平移动画。
- 预览内容加载策略调整：文本、颜色、图片立即随窗口动画加载；链接 WebView 仅保留极短延迟，降低可见等待。
- 新增发布前 smoke check 脚本，检查版本号格式、菜单栏模式、关键文档章节、关键源码标记和 App bundle 基础结构。
- 项目指引、README 和第一版回归测试清单补充 `scripts/smoke_check.py`，作为发布前自动基础检查。
- 新增第一版发布说明，整理系统要求、功能范围、权限说明、发布前验证命令和数据安全策略。
- 新增已知限制文档，记录 iCloud 同步、OCR、加密、WebKit 链接预览和 SQLite 迁移等非阻塞后续事项。
- 项目指引、README 和 smoke check 已接入发布说明与已知限制文档。
- 新增发布候选流程文档，固定自动检查、手动回归、RC 构建和 RC 后处理规则。
- 新增第一版 RC 检查报告，记录 `1.0.0` 候选包自动检查结果、待用户确认项和包信息。
- 已按 major 规则构建并启动 `1.0.0(260512.2134)` 第一版候选包。
- 修复 RC 阻塞问题：主窗口横向滚动位置会在关闭后保留，再次打开恢复到用户上次浏览位置。
- 优化主窗口显示动画：避免每次显示都将列表重置为首屏限制后再展开，减少弹出期间内容重绘导致的卡顿。
- 文字搜索命中样式改为黄色背景，并支持同一段文本内多处命中同时高亮。
- 收口第一版 RC 文档：发布候选报告、发布候选流程、发布说明、回归测试清单和 README 已统一到 `1.0.x` RC 修复轮规则。
- 增强发布前 smoke check：检查 App bundle 版本与源码 Info.plist 一致，并检查 RC 报告包含当前版本号和构建号。
- 第一版已由用户确认测试无问题，第二版进入方案头脑风暴阶段；新增第二版产品方案、技术方案草案和开发计划草案，记录已确认的性能与管理优先、收藏/置顶分离、文件夹式分组方向。
- 第二版方案继续细化：确认置顶清理、取消收藏、收藏到分组、普通历史编辑、多选管理模式、敏感遮罩策略，并记录富文本卡片展示、新历史回到第一条、菜单快捷键、窗口层级、新建文本入口隐藏主窗口和全局提示层建议。

## 验证

- 已执行 `swift build`，设置窗口隐藏式关闭调试构建通过。
- 已执行 `swift build`，统一权限状态、历史窗口/设置页权限状态共享调试构建通过。
- 已执行 `swift build`，权限页 App 路径诊断和 Finder 定位调试构建通过。
- 已执行 `swift build`，粘贴结果提示和失败保护调试构建通过。
- 已执行 `swift build`，自动粘贴焦点恢复调试构建通过。
- 已执行 `swift build`，自动粘贴目标进程投递调试构建通过。
- 已执行 `swift build`，历史窗口非激活显示和焦点保留粘贴策略调试构建通过。
- 已执行 `swift build`，历史窗口非激活键盘事件路由调试构建通过。
- 已执行 `swift build`，搜索输入模式和焦点恢复调试构建通过。
- 已执行 `swift build`，搜索结果选择、空结果保护和 Esc 搜索退出调试构建通过。
- 已执行 `swift build`，输入即搜索事件路由调试构建通过。
- 已执行 `swift build`，搜索 Esc 分阶段处理和清空按钮行为调试构建通过。
- 已执行 `swift build`，输入即搜索选区、Esc、Delete 和 `Command + A` 修复调试构建通过。
- 已执行 `swift build`，搜索框第一响应者焦点和面板级 Esc 路由调试构建通过。
- 已执行 `swift build`，搜索框原生编辑快捷键、末尾进入结果和新搜索替换逻辑调试构建通过。
- 已执行 `swift build`，搜索框展开期间快捷键优先交给原生文本框调试构建通过。
- 已执行 `swift build`，搜索框焦点快捷键、`Command + 1-9` 直接粘贴和外部点击关闭调试构建通过。
- 已执行 `swift build`，搜索框未聚焦时 Esc 先关闭搜索框调试构建通过。
- 已执行 `swift build`，搜索防抖、后台过滤和延迟预热调试构建通过。
- 已执行 `swift build && scripts/build-app.sh --run`，搜索框未聚焦 Esc 清空并关闭搜索框修复已编译启动。
- 已执行 `swift build`，搜索快捷键状态机、主窗口新增快捷键和卡片渲染上限调试构建通过。
- 已执行 `swift build`，完整结果恢复、搜索框原生快捷键和无动画结果刷新调试构建通过。
- 已执行 `swift build`，取消历史硬上限和开发测试数据入口调试构建通过。
- 已执行 `swift build`，历史保存防抖和测试文本清理入口调试构建通过。
- 已执行 `swift build`，后台生成测试文本和测试数量显示调试构建通过。
- 已执行 `swift build`，后台构建历史预览项和搜索索引调试构建通过。
- 已执行 `swift build`，孤立附件清理工具和设置页入口调试构建通过。
- 已更新版本规则文档，默认正式打包从只更新时间戳改为递增 patch 版本。
- 已更新版本脚本和文档，时间戳改为任何打包都必须更新。
- 已执行 `swift build`，设置页后台存储用量计算、后台孤立附件清理和确认弹窗调试构建通过。
- 已执行 `swift build`，设置页导入导出失败原因展示调试构建通过。
- 已执行 `swift build`，设置页导入导出和备份包后台任务调试构建通过。
- 已执行 `swift build`，备份包导入完整性校验和结果统计调试构建通过。
- 已执行 `swift build`，设置页数据健康检查调试构建通过。
- 已执行 `swift build`，数据健康检查详情弹窗调试构建通过。
- 已补充第一版回归测试清单文档和 README 项目状态说明。
- 已执行 `swift build`，第一版发布前阻塞问题修复调试构建通过。
- 已执行 `swift build`，新建文本多窗口/快捷键和链接识别/网页预览修复调试构建通过。
- 已执行 `swift build`，预览窗口按屏幕比例缩放、文本测量尺寸和图片滚动预览调试构建通过。
- 已执行 `swift build`，预览窗口定位和文本自动换行高度测量修复调试构建通过。
- 已执行 `swift build`，预览窗口延迟加载重内容和关闭卸载重内容优化调试构建通过。
- 已执行 `swift build`，长文本轻量估算、图片等比适配、主窗口滚轮和预览动画节奏调整调试构建通过。
- 已执行 `swift build`，主窗口滚轮查找、图片内容区缩放、文本分块加载和 Esc 预览优先关闭调试构建通过。
- 已执行 `swift build`，滚轮速度、边缘卡片完整显示和图片预览比例高度修复调试构建通过。
- 已执行 `swift build && scripts/build-app.sh --run`，图片预览内边距和滚轮方向/速度修复已编译启动。
- 已执行 `swift build && scripts/build-app.sh --run`，图片预览上下间隙修复已编译启动。
- 已执行 `swift build`，边缘卡片平移动画和预览内容即时加载策略调试构建通过。
- 已执行 `scripts/smoke_check.py`，发布前基础检查通过。
- 已补充第一版发布资料，等待自动检查和打包验证。
- 已执行 `swift build`，RC 调试构建通过。
- 已执行 `scripts/smoke_check.py`，RC 自动基础检查通过。
- 已执行 `scripts/build-app.sh --bump major --run`，`1.0.0(260512.2134)` RC 包已构建启动。
- 已执行 `swift build`，滚动位置记忆和主窗口动画减重修复调试构建通过。
- 已执行 `swift build && scripts/smoke_check.py && scripts/build-app.sh --bump patch --run && scripts/smoke_check.py`，`1.0.2(260512.2205)` 已构建启动，搜索黄色高亮修复验证通过。
- 已执行 `scripts/smoke_check.py && swift build && scripts/build-app.sh --bump patch --run`，`1.0.3(260512.2215)` 已构建启动，RC 文档收口验证通过。
- 已执行 `scripts/smoke_check.py`，增强后的 RC 版本一致性检查通过。
- 已执行 `swift build && scripts/build-app.sh --bump patch --run`，`1.0.4(260512.2222)` 已构建启动，等待最终 smoke check。
- 第二版本轮只更新文档方案，未实施功能代码。
- 第二版细节头脑风暴本轮只更新方案文档，未实施功能代码。

## 待办

- 编译启动当前修复版本，进行发布前手动回归测试。


## 今日目标

- 继续第二版功能头脑风暴，只沉淀方案，不实施功能代码。

## 已完成

- 确认链接卡片 `Command + E` 编辑 URL 地址，并参考 Paste 的链接编辑窗口。
- 确认链接编辑窗口底部“在 默认浏览器 中打开”按钮需要按系统默认浏览器动态显示，例如 Chrome 或 Edge。
- 确认收藏 / 取消收藏快捷键为 `Command + D`。
- 确认管理模式快捷键为 `Command + M`。
- 确认全局提示层显示来源 App 图标和短提示。
- 确认富文本卡片按复制的格式显示。

## 验证结果

- 本轮为方案文档更新，未实施功能代码，未打包新版本。

## 遇到的问题

- 暂无阻塞。

## 明日待办

- 继续确认第二版分组入口、收藏排序、敏感遮罩默认状态和分组视觉细节。

## 决策记录

- 2026-05-12：链接编辑以 URL 地址为核心，不先扩展标题/备注编辑，保持和 Paste 的核心编辑体验一致。
